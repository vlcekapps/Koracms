# Manual Accessibility Test Protocol

Tento protokol popisuje ruční ověření pro `wcag-22-aa-conformance.md`. Ruční test je povinný pro kritéria, která nejde spolehlivě uzavřít čistě statickým auditem.

Aktuální ruční evidence:

- 2026-07-04: čistý HTML editor dostal helper `Jazyk části textu` pro vložení `<span lang="…">` kolem vybraného úseku. Runtime audit hlídá zdrojový markup, JS a CSS; od 2026-07-10 guardrail pokrývá i veřejně renderované taxonomické HTML popisy. Ručně zbývá ověřit použití helperu s klávesnicí/NVDA a kvalitu skutečného author contentu.
- 2026-07-04: vznikl `author-content-checklist.md` pro redakční kontrolu alt textů, médií, titulků, přepisů, jazyka částí, odkazů, nadpisů, tabulek, barev a externích embedů. Ručně zbývá projít reprezentativní publikovaný obsah a potvrdit, že nálezy jsou správně tříděné na core CMS defect, theme defect a author-content issue.
- 2026-07-04: browser průchod při 320 px ověřil admin stránky media, widgets, statistics, Form Builder, přehled formulářů, comments, contact, chat, reservations, food, downloads, gallery, importy, content picker a reprezentativní dlouhé formuláře page/blog/news/event/download/gallery/food/board/FAQ/place/polls/reservations/podcast. Nalezený horizontální scroll rootu ve statistikách, přehledu formulářů, food/downloads/gallery a podcastech i main-level scroll u contact/chat a dlouhých fieldsetů byl opravený přes `.table-responsive`, posílené CSS containment, wrapper skrytých datových tabulek grafů a sdílené CSS pro fieldset/form controls; admin hledání mělo viditelný focus a ověřené samostatné cíle neměly pod 24 px. Zbylý ruční průchod se týká hlavně 400 % zoomu, custom modulů, sticky/anchor skoků a keyboard-only/NVDA kombinací.
- 2026-07-05: audio/video snippety podporují odkaz na přepis přes `transcript`; přímý video shortcode podporuje WebVTT titulky přes `captions`, `srclang` a `caption_label` i audio-description stopu přes `descriptions` a `description_label`; knihovna médií přijímá `.vtt` soubory. Unit a runtime testy hlídají HTML výstup; ručně zbývá ověřit timing, jazyk, kvalitu titulků/přepisů/popisu a chování v prohlížeči se čtečkou.
- 2026-07-04: podcastové epizody mají modelované pole `transcript` pro textovou alternativu audia; unit testy helperů a runtime veřejného detailu hlídají, že se přepis vykreslí jako samostatná sekce. Ručně zbývá ověřit kvalitu skutečných přepisů a další média mimo podcastový modul.
- 2026-07-04: runtime `contrast_focus_guardrails` automaticky měří baseline kontrast pro default theme, admin layout a standalone login: textové páry, stavové hlášky, skip link, focus tokeny a hranice inputů/tlačítek. Ruční kontrastní průchod zůstává povinný pro custom theme settings, hover/disabled stavy, ikony a progress bary.
- 2026-07-04: auth flow pro WCAG 2.2 `3.3.8 Accessible Authentication (Minimum)` potvrzen jako funkční se správcem hesel, TOTP jednorázovým kódem, tokenovým resetem a chybovými stavy; opakovat při změnách auth/session chování.
- 2026-07-06: re-auth ochrana dlouhých admin editací pro WCAG `2.2.1 Timing Adjustable` má automatizovaný guardrail: autosave drží recovery kopii při submitu, content-lock heartbeat oznamuje 401 přes live region a admin login oznamuje návrat na původní stránku i možnost lokálního záložního konceptu. Ručně zbývá projít reálný session timeout s NVDA/keyboard-only, 2FA pending session, více panelů a stav bez dostupného localStorage.
- 2026-07-04: command centrum, widget dialog a content/media picker potvrzené bez regrese při ručním keyboard-only/NVDA průchodu; opakovat při změnách JS dialogů, focus trapu nebo admin layoutu.
- 2026-07-05: administrace má jednotnou stránku `Nápověda a podpora` ve stabilní spodní navigaci. Runtime `admin_consistent_help_guardrails` a HTTP `admin_consistent_help_http` hlídají link order, read-only route guard, nadpisové sekce a inventář podpory; ručně zbývá potvrdit průchod s klávesnicí/NVDA a veřejné/theme help mechanismy.
- 2026-07-05: výchozí veřejná šablona má ve footeru navigaci `Pomoc a kontakt` pro Kontakt a Chat podle zapnutých modulů. Runtime `public_consistent_help_guardrails` a HTTP `public_consistent_help_http` hlídají default public baseline; od 2026-07-10 stejné guardraily pokrývají i bundled/portable static theme inheritance. Ručně zbývá potvrdit čtení a focus ve footeru a samostatně projít případné provozní PHP úpravy šablony mimo portable model.
- 2026-07-06: automatizované guardraily pro WCAG 2.2 `1.3.5 Identify Input Purpose` pokrývají auth flow, veřejný kontakt, food objednávky, guest rezervace a Form Builder renderer včetně jména, e-mailu, telefonu, URL, organizace, pracovní pozice, data narození a jasně pojmenovaných adresních polí. Ručně zbývá ověřit, že Firefox/Chrome a používaný správce hesel nebo autofill tato metadata skutečně nabízejí bez matoucích návrhů.
- 2026-07-08: automatizovaný guardrail pro WCAG `3.3.4 Error Prevention` pokrývá trvalé smazání v administrativním Koši, změnu role existujícího uživatelského účtu, individuální mazání uživatelského účtu, individuální mazání nepoužitého média, odeslání newsletter rozesílky, hromadné akce nad odběrateli newsletteru, datově dopadající importy, stavové změny rezervací v administraci, ruční SQL zálohu databáze, hlavní JSON export CMS, CSV export odpovědí Form Builderu, CSV export statistik obsahu, hromadné akce nad odpověďmi Form Builderu, hromadné smazání/ZIP export alb galerie, ZIP export šablony a sdílené hromadné mazání běžných admin modulů přes `bulkActions()`. HTTP scénáře ověřují review text u konkrétní položky, povinný potvrzovací checkbox, serverové odmítnutí nepotvrzeného purge, potvrzené trvalé smazání, review aktuální/nové role, odmítnutí nepotvrzené změny role, uloženou potvrzenou změnu, odmítnutí nepotvrzeného smazání uživatele bez změny účtu/zkratky/logu, potvrzené smazání uživatele s cleanupem zkratek, odmítnutí nepotvrzeného smazání média bez změny DB záznamu, fyzického souboru nebo audit logu, potvrzené smazání média s cleanupem souboru, review počtu příjemců newsletteru, odmítnutí nepotvrzené rozesílky bez zápisu do historie, odmítnutí nepotvrzeného bulk delete bez smazání odběratele, odmítnutí validního JSON importu bez změny nastavení nebo newsletter historie, odmítnutí nepotvrzeného schválení rezervace bez změny stavu nebo historie, odmítnutí nepotvrzené SQL zálohy bez stažení nebo audit logu, odmítnutí nepotvrzeného JSON exportu bez attachmentu nebo audit logu, odmítnutí nepotvrzeného CSV exportu odpovědí bez attachmentu nebo audit logu, odmítnutí nepotvrzeného CSV exportu statistik obsahu bez attachmentu nebo audit logu, odmítnutí nepotvrzeného bulk delete odpovědi bez smazání odpovědi nebo audit logu, odmítnutí nepotvrzeného ZIP exportu galerie bez attachmentu/logu, odmítnutí nepotvrzeného ZIP exportu šablony bez attachmentu/logu, odmítnutí nepotvrzeného bulk delete alba bez smazání/logu a odmítnutí nepotvrzeného FAQ bulk delete bez smazání otázky/logu. Ručně zbývá produktově projít další právně/datově kritické akce mimo koš, role, individuální mazání uživatelů, média, importy, stavové změny rezervací, SQL zálohu, hlavní JSON export CMS, CSV export odpovědí Form Builderu, CSV export statistik obsahu, ZIP export šablony, hromadné akce odpovědí Form Builderu, hromadné akce galerie a sdílené generic bulk delete flow.
- 2026-07-09: trvalé smazání v Koši má zpřesněnou automatizovanou evidenci chybového stavu: nepotvrzený purge se vrací na konkrétní položku, checkbox `confirm_permanent_delete` má `aria-invalid`, `aria-describedby` na review text a field-level chybu a HTTP ověřuje, že bez potvrzení nevznikne `trash_purge` audit log. Ručně zbývá ověřit, že NVDA/keyboard-only po návratu oznámí alert, konkrétní checkbox, review text a lokální chybu srozumitelně u jedné položky v delším koši.
- 2026-07-08: individuální mazání odběratele newsletteru má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled i detail odběratele zobrazují review dopadu na aktivní odběr a zachování historie rozesílek, nepotvrzený POST bez `confirm_newsletter_subscriber_delete_<id>` vrací atomický alert a field-level chybu bez smazání odběratele nebo audit logu a potvrzený POST projde přes PRG stav. Ruční průchod newsletteru potvrzený 2026-07-09 pokrývá čtení review textu, checkboxu a opravy chyby; opakovat při změnách newsletter workflow.
- 2026-07-09: newsletter workflow je ručně potvrzené bez zjištěné regrese. Evidence navazuje na automatizované guardraily pro veřejný odběr, captcha chyby, tokenové potvrzení/odhlášení, administrační composer, hromadné akce a individuální mazání odběratele; další ruční kolo je potřeba až při změně newsletteru nebo sdílených stavových hlášek.
- 2026-07-09: širší ruční NVDA průchod bez nahlášené regrese pokryl blogy, nastavení, editaci dlouhých formulářů, odstraňování a přeskupování widgetů, statické stránky i stránky blogu, statistiky, hledání v administraci včetně command palety, focus order a připínání/odepínání položek z osobního dashboardu. Evidence uzavírá samostatné screen-reader follow-upy pro tato workflow v aktuálním ACR draftu; opakovat při změnách blogu, nastavení, widgetů, stránek, statistik, command centra, pořadí fokusu nebo osobních zkratek. Vizuální 400% zoom, kontrast, hover/ikonové stavy a custom theme varianty zůstávají samostatné ruční oblasti.
- 2026-07-09: mazání témat kontaktu a chatu má samostatný automatizovaný guardrail pro WCAG `3.3.4`: řádkové akce zobrazují review dopadu na veřejný kontaktní formulář nebo chat a existující zprávy, nepotvrzený POST bez `confirm_contact_topic_delete_<id>` nebo `confirm_chat_topic_delete_<id>` vrací atomický alert a field-level chybu bez smazání tématu, odpojení vazby zprávy nebo audit logu a potvrzený POST projde přes PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání rezervačních kategorií a míst má samostatný automatizovaný guardrail pro WCAG `3.3.4`: řádkové akce zobrazují review počtu navázaných rezervačních zdrojů a dopadu na existující zdroje/rezervace, nepotvrzený POST bez `confirm_res_category_delete_<id>` nebo `confirm_res_location_delete_<id>` vrací atomický alert a field-level chybu bez smazání číselníku, odpojení vazby zdroje nebo audit logu a potvrzený POST projde přes PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání kategorií vývěsky a typů akcí má samostatný automatizovaný guardrail pro WCAG `3.3.4`: řádkové akce zobrazují review počtu navázaných položek/odběrů nebo událostí, nepotvrzený POST bez `confirm_board_category_delete_<id>` nebo `confirm_event_type_delete_<id>` vrací atomický alert a field-level chybu bez smazání taxonomie, odpojení vazby nebo audit logu a potvrzený POST projde přes PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání blogových kategorií, štítků a sérií článků má samostatný automatizovaný guardrail pro WCAG `3.3.4`: řádkové akce zobrazují review počtu navázaných článků a u kategorií i podkategorií, nepotvrzený POST bez `confirm_blog_category_delete_<id>`, `confirm_blog_tag_delete_<id>` nebo `confirm_blog_series_delete_<id>` vrací atomický alert a field-level chybu bez smazání taxonomie, odpojení článků, podkategorií nebo sériových/štítkových vazeb a bez audit logu. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání FAQ kategorií má samostatný automatizovaný guardrail pro WCAG `3.3.4`: řádkové akce zobrazují review počtu navázaných otázek a podkategorií, nepotvrzený POST bez `confirm_faq_category_delete_<id>` vrací atomický alert a field-level chybu bez smazání kategorie, odpojení otázek, přesunu podkategorií nebo audit logu a potvrzený POST projde přes PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání celého blogu má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled blogů zobrazuje review počtu článků, kategorií, štítků, sérií a týmových přiřazení, nepotvrzený POST bez `confirm_blog_delete_<id>` vrací atomický alert a field-level chybu bez přesunu obsahu, smazání sérií, zrušení týmu, smazání blogu nebo audit logu. Potvrzený průchod běží v transakci a HTTP ověřuje přesun obsahu do fallback blogu. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání rezervačního zdroje má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled zdrojů zobrazuje review počtu budoucích nezrušených rezervací, vazeb na místa, pravidel otevírací doby, slotů a blokovaných dnů, nepotvrzený POST bez `confirm_res_resource_delete_<id>` vrací atomický alert a field-level chybu bez zrušení rezervací, smazání dostupnosti, zdroje nebo audit logu. Potvrzený průchod běží v transakci a HTTP ověřuje zrušení budoucí rezervace i cleanup dostupnosti. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: mazání celého Form Builder formuláře má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled formulářů zobrazuje review veřejné URL, počtu polí, odpovědí, historie odpovědí a dopadu na přílohy, nepotvrzený POST bez `confirm_form_delete_<id>` vrací atomický alert a field-level chybu bez smazání formuláře, polí, odpovědí, historie nebo audit logu. Potvrzený průchod maže databázová data v transakci a HTTP ověřuje cleanup. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: individuální mazání odpovědi Form Builderu má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled odpovědí i detail odpovědi zobrazují review reference, stavu, interní historie a příloh, nepotvrzený POST bez `confirm_form_submission_delete_<id>` vrací atomický alert a field-level chybu bez smazání odpovědi, historie, příloh nebo audit logu. Potvrzený průchod maže databázová data v transakci a HTTP ověřuje přehled, detail i cleanup. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-09: odebrání widgetu má samostatný automatizovaný guardrail pro WCAG `3.3.4`: přehled widgetů zobrazuje review názvu, typu, zóny a dopadu na veřejné zobrazení, nepotvrzený POST bez `confirm_widget_delete_<id>` vrací atomický alert a field-level chybu bez smazání widgetu nebo audit logu a potvrzený POST projde přes PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-06: CSV export agregovaných výsledků anket má pro WCAG `3.3.4` samostatný review krok: GET zobrazí téma ankety, typ hlasování, viditelnost výsledků, počet hlasujících a vybraných odpovědí, nepotvrzený POST s chybějícím `confirm_poll_results_csv_export` vrátí atomický alert a field-level chybu bez attachmentu nebo audit logu a potvrzený POST stáhne bezpečný CSV attachment bez raw IP hashů nebo voter hashů. Ručně zbývá ověřit oznámení review textu a chybového stavu s NVDA/keyboard-only.
- 2026-07-08: CSV export statistik obsahu má pro WCAG `3.3.4` samostatný review krok: GET zobrazí interní metriky výkonu obsahu, období, filtr modulu, počet řádků a upozornění na redakční/provozní citlivost agregovaných dat, nepotvrzený POST s chybějícím `confirm_statistics_content_csv_export` vrátí atomický alert a field-level chybu bez attachmentu nebo audit logu a potvrzený POST stáhne bezpečný CSV attachment bez raw IP hashů, user-agentů nebo referrer tokenů. Ručně zbývá ověřit oznámení review textu a chybového stavu s NVDA/keyboard-only.
- 2026-07-09: CSV export audit logu má pro WCAG `3.3.4` samostatný review krok: GET zobrazí citlivost provozních a bezpečnostních záznamů, aktuální filtr a počet řádků, nepotvrzený POST s chybějícím `confirm_audit_log_csv_export` vrátí atomický alert a field-level chybu bez attachmentu nebo audit logu a potvrzený POST stáhne bezpečný CSV attachment. Ručně zbývá ověřit oznámení review textu a chybového stavu s NVDA/keyboard-only.
- 2026-07-06: ZIP export šablony má pro WCAG `3.3.4` samostatný review krok: správa šablon popíše přenos manifestu, uložené vizuální konfigurace a statických assetů, nepotvrzený POST s chybějícím `confirm_theme_export` vrátí atomický alert a field-level chybu bez attachmentu nebo audit logu a potvrzený POST stáhne bezpečný ZIP attachment. Ručně zbývá ověřit oznámení review textu a chybového stavu s NVDA/keyboard-only.
- 2026-07-06: mazání jednotlivého přesměrování má pro WCAG `3.3.4` samostatný review krok: řádková akce používá POST formulář, review staré/nové cesty, checkbox `confirm_redirect_delete_<id>`, textový alert a field-level chybu. HTTP ověřuje, že bez potvrzení se redirect nesmaže ani nezapíše audit log a s potvrzením projde PRG stav. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-06: mazání uživatelského účtu má pro WCAG `3.3.4` samostatný review krok: řádková akce používá POST formulář, review dopadu na přístup do CMS a osobní administrační zkratky, checkbox `confirm_user_delete_<id>`, textový alert a field-level chybu. HTTP ověřuje, že bez potvrzení se účet nesmaže, zkratka zůstane a audit log se nezapíše; s potvrzením se účet i osobní zkratka odstraní a vznikne audit log `user_delete`. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-06: mazání nepoužitého média má pro WCAG `3.3.4` samostatný review krok: karta média popisuje odstranění souboru, metadat a odvozených miniatur, checkbox `confirm_media_delete_<id>`, textový alert a field-level chybu. HTTP ověřuje, že bez potvrzení se nesmaže DB záznam, fyzický soubor ani audit log; s potvrzením se médium odstraní a vznikne audit log `media_delete`. Ručně zbývá ověřit čtení review textu, checkboxu a opravy chyby s NVDA/keyboard-only.
- 2026-07-06: automatizovaný guardrail pro WCAG 2.2 `3.3.7 Redundant Entry` pokrývá veřejný kontakt, Food objednávku, přihlášené veřejné rezervace, veřejný chat, odpovědi v chatu a blogové komentáře: `currentUserContactDefaults()` čte jméno, e-mail a telefon z profilu, HTTP integrace ověřuje předvyplnění v renderu kontaktu/Food/chatu/blogového komentáře, zachování ručně upravených POST hodnot po chybě a u rezervací také uložení kontaktního snapshotu bez opakované kontaktní výzvy nebo captchy. Ručně zbývá projít širší auth/objednávková/custom flow, okrajové rezervační scénáře a potvrdit, že předvyplnění nebo vynechání opakovaného zadání není matoucí na sdíleném zařízení.
- 2026-07-04: runtime `text_spacing_guardrails` hlídá core CSS proti zápornému `letter-spacing`, textovému ořezu přes ellipsis/line clamp a `!important` zámkům na text-spacing vlastnostech. Ručně zbývá browser průchod s text-spacing override.
- 2026-07-04: runtime `public_error_suggestion_guardrails` a HTTP integrace hlídají, že veřejná matematická ověřovací otázka v kontaktu, newsletteru, odběru vývěsky, Food objednávkách, guest rezervacích a Form Builderu vrací field-level chybu s konkrétním návrhem opravy. Guest rezervace navíc mapují chyby jména, e-mailu, telefonu, počtu osob a slotu/času na konkrétní ovládací prvky. Ručně zbývá širší copy pass administračních a custom validačních hlášek.
- 2026-07-08: blog má modulovou přílohu `docs/accessibility/modules/blog.md`. Veřejný komentářový formulář u článku má automatický HTTP důkaz pro souhrnný alert, field-level chyby jména, e-mailu, textu komentáře a captchy i sdílenou akční captcha hlášku; ručně zbývá projít celý blogový detail s NVDA/Firefox, dlouhou osnovou, sérií a souvisejícími články.
- 2026-07-04: unit testy Form Builder error suggestions, runtime `public_forms_http_guardrails` a HTTP `public_form_submit_http` hlídají, že custom povinná, e-mailová, URL, výběrová a upload pole Form Builderu vrací konkrétní field-level návrhy oprav a po neúspěšném odeslání nezanechají uloženou neplatnou přílohu.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá, že URL chyby v administraci míst a podcastů radí použít http/https adresu, doménu bez schématu nebo prázdné volitelné pole místo obecného „platný formát“.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také plánovací datum/čas chyby u stránek, článků, novinek, událostí, anket, rezervačních zdrojů a podcastových epizod. Text musí poradit výběr platné hodnoty v poli datum/čas, prázdné volitelné plánování, odstranění prázdného řádku nebo opravu pořadí začátku/konce.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také vybrané administrační e-mailové chyby. Text musí poradit úplnou adresu ve tvaru `jmeno@example.cz`, prázdné volitelné pole nebo jedinečnost přihlašovací adresy.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také běžné datumové chyby ve vývěsce, jídelních lístcích a položkách ke stažení. Text musí poradit výběr kalendářního data, prázdné volitelné pole nebo opravu pořadí od/do; datum vydání u downloadů musí mít field-level chybu přes `aria-describedby`.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také zdrojové chyby položek ke stažení. Text musí poradit lokální soubor nebo externí odkaz, http/https nebo doménový tvar URL, prázdné volitelné URL pole a přesný 64znakový SHA-256 checksum; field-level chyby musí být přes `aria-describedby` navázané na existující prvky.
- 2026-07-05: editor rezervačního zdroje už pro klientské chyby hromadného generátoru slotů a přidání blokovaného dne nepoužívá prohlížečový `alert()`. Runtime `admin_field_error_guardrails` hlídá inline textové `role="alert"` prvky, konkrétní návrhy oprav, focus na dotčené pole a zákaz návratu starých alertů.
- 2026-07-05: sdílené administrační AJAX řazení už při uložení nebo selhání nevyvolává prohlížečový `alert()`. Runtime `admin_field_error_guardrails` hlídá vloženou textovou hlášku s `role="status"` / `role="alert"`, `aria-atomic="true"`, konkrétní retry text při selhání a zákaz návratu starého alertu.
- 2026-07-05: runtime `admin_field_error_guardrails` hlídá také reply formuláře v detailech kontaktu, chatu a odpovědi Form Builderu. Při prázdném předmětu nebo textu odpovědi musí být souhrnný alert doplněný field-level chybou u obou polí přes existující `aria-describedby`, `aria-invalid` a konkrétní návrh opravy.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `form_issue_preset_http` hlídají také GitHub issue bridge v detailu odpovědi Form Builderu. Chyba vytvoření issue musí navázat repozitář, název a tělo issue na field-level text; chyba ručního napojení musí navázat URL existující issue. Všechny odkazy přes `aria-describedby` musí mířit na existující prvky.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `form_issue_preset_http` hlídají také hlavní chybové stavy editoru Form Builderu. Název, slug, notifikační e-mail, pole pro potvrzovací e-mail odesílateli a webhook URL musí mít souhrnný alert, `aria-invalid`, existující `aria-describedby` a konkrétní návrh opravy.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP scénář blogových statických stránek hlídají také hlavní chybové stavy editoru statických stránek. Název, slug, přiřazený blog, plánované publikování a plánované zrušení publikace musí mít souhrnný alert, `aria-invalid`, existující `aria-describedby` a konkrétní návrh opravy.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `faq_categories_feedback_http` hlídají také hlavní chybové stavy editoru FAQ. Otázka, odpověď a slug veřejné stránky musí mít souhrnný atomický alert, field-level text a konkrétní návrh opravy; odkazy přes `aria-describedby` musí mířit na existující prvky.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `faq_categories_feedback_http` hlídají také hlavní chybové stavy editoru FAQ kategorií. Název, slug a meta title musí mít souhrnný atomický alert, field-level text a konkrétní návrh opravy; odkazy přes `aria-describedby` musí mířit na existující prvky.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `blog_taxonomy_landing_http` hlídají také hlavní chybové stavy blogových kategorií a štítků. Povinný název a duplicitní slug musí mít souhrnný atomický alert, field-level text a konkrétní návrh opravy; odkazy přes `aria-describedby` musí mířit na existující prvky.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `downloads_catalog_versions_http` hlídají také hlavní chybové stavy kategorií a sérií ke stažení. Prázdný název, nepoužitelný nebo duplicitní slug a příliš dlouhý meta title kategorie musí mít souhrnný atomický alert, field-level text a konkrétní návrh opravy; odkazy přes `aria-describedby` musí mířit na existující prvky.
- 2026-07-08: runtime `downloads_source_guardrails` a HTTP `downloads_catalog_versions_http` hlídají také mazání kategorií a sérií Ke stažení. Formuláře musí mít review počtu navázaných položek, u série i aktuálních verzí, checkbox `confirm_download_category_delete_<id>` nebo `confirm_download_series_delete_<id>`, textový alert a field-level chybu u checkboxu při chybějícím potvrzení; nepotvrzený POST nesmí změnit vazby ani audit log.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP scénáře `board_save_http`, `events_types_places_recurrence_http`, `contact_topics_and_reply_http`, `chat_topics_threads_support_http` a `reservations_http` hlídají také kategorie vývěsky, typy akcí, témata kontaktu/chatu a rezervační kategorie/místa. Povinné názvy, nepoužitelné nebo duplicitní slugy a příliš dlouhý meta title musí mít souhrnný atomický alert, field-level text, existující `aria-describedby` cíle a zachované hodnoty tam, kde se po chybě formulář znovu vykreslí.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP scénáře `blog_article_series_http`, `blog_static_pages_http`, `reservations_http`, `podcast_admin_validation_http` a `newsletter_compose_validation_http` hlídají také série článků, externí odkazy blogu, základní chyby rezervačních zdrojů, podcastové pořady a newsletter composer. Chybové stavy musí mít atomický souhrnný alert, `aria-invalid`, existující `aria-describedby` cíle a konkrétní field-level návrh opravy.
- 2026-07-10: runtime `podcast_source_guardrails` a HTTP `podcast_admin_validation_http` nově pokrývají také editory kapitol, osob a platforem podcastu. Serverové chybové stavy musí zachovat odeslané hodnoty, obejít obecnou nativní URL validaci přes `novalidate`, navázat dotčené pole na existující field-level text a poradit přijatelný čas, název, http/https nebo doménový tvar, prázdné volitelné URL pole nebo úpravu existující duplicitní položky.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP scénáře `forms_nav_http`, `redirects_validation_http`, `gallery_metadata_http` a `reservations_http` hlídají také hlavní navigační externí odkazy, přesměrování, alba galerie, fotografie galerie a ruční vytvoření rezervace. Chyby názvu, interní cesty/URL, slugu, nadřazeného alba, licence, data pořízení, zdroje, uživatele/hosta, data/času a konfliktu rezervace musí mít atomický alert, `aria-invalid`, existující `aria-describedby` cíle, konkrétní návrh opravy a zachované hodnoty tam, kde se formulář po chybě znovu vykreslí.
- 2026-07-06: runtime `contrast_focus_guardrails` a `admin_mobile_reflow_guardrails` hlídají další vizuální admin baseline: dark-mode info panely a inline štítky, forced-colors fallback, focus/anchor scroll-margin, disabled ovládací prvky bez opacity dimmingu, minimální checkbox/radio velikost, 28px řadicí cíle a mobilní kolaps theme/checkbox/summary gridů i vybraných flex řádků. Ruční kolo se proto soustředí hlavně na reálný 400% zoom, forced-colors v prohlížeči, custom moduly, ikony, progress bary a sticky/anchor kombinace.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP scénáře `blog_related_articles_http`, `blog_management_validation_http`, `board_save_http`, `food_structured_items_http` a `poll_voting_modes_http` hlídají také editor článku, správu blogů, položky vývěsky, jídelní/nápojové lístky a ankety. Povinné hodnoty, slugy, cizí taxonomie/série, datum vyvěšení, kategorie, limity možností a vícevýběrové ankety musí mít atomický alert, field-level text s konkrétním návrhem opravy, existující `aria-describedby` cíle a zachované hodnoty tam, kde se formulář po chybě znovu vykreslí.
- 2026-07-05: runtime `admin_field_error_guardrails` a HTTP `admin_validation_a11y_http` hlídají také profil správce, správu uživatelských účtů, nastavení webu a správu šablon. E-mail, heslo, kontrolní heslo, autor, TOTP kód, název webu, veřejný název vývěsky, GitHub repozitář, logo/favicona, aktivní šablona, theme settings a theme ZIP import/export musí mít atomický alert, field-level text, existující `aria-describedby` cíle a konkrétní návrh opravy.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také vybrané obrazové upload chyby v článcích, vývěsce, událostech, místech a downloadech. Text musí poradit JPEG/PNG/GIF/WebP, zákaz SVG a prázdné volitelné pole; download náhled musí mít field-level chybu přes existující `aria-describedby`.
- 2026-07-04: runtime `admin_field_error_guardrails` hlídá také neobrazové upload chyby u přílohy vývěsky a audio souboru podcastové epizody. Text musí poradit povolené dokumentové/audio formáty, prázdné volitelné pole a u podcastu alternativu externího audio odkazu.
- 2026-07-08: při ručním testu uploadů ověřte, že limit v obecných nastaveních CMS mění nápovědu a validační texty knihovny médií, hromadného uploadu galerie a výchozího limitu nových souborových polí Form Builderu; PHP `upload_max_filesize` a `post_max_size` musí být na hostingu alespoň stejně vysoké.
- 2026-07-04: runtime `media_library_guardrails` hlídá, že hromadný upload a náhrada souboru v knihovně médií po redirectu nezůstávají jen ve flash alertu. File input musí dostat field-level chybu přes existující `aria-describedby` a text musí poradit podporovaný formát do nastaveného limitu uploadu, zákaz SVG, u náhrady stejnou MIME rodinu a u veřejných souborů stejnou příponu.
- 2026-07-04: runtime `admin_field_error_guardrails` a HTTP `admin_import_error_suggestions_http` hlídají importní formuláře. JSON import, WordPress WXR, eStránky XML a downloader fotografií musí po chybě zobrazit alert, field-level chybu přes existující `aria-describedby` a konkrétní opravu pro exportní soubor, UTF-8 nebo URL webu.

## Prostředí

- Windows + Firefox + NVDA.
- Windows + Chrome nebo Edge bez čtečky pro keyboard-only a zoom.
- Zoom 200 % a 400 %.
- Viewport 320 px šířky a běžný desktop.
- Vypnutá myš nebo test bez použití myši.

Pro WCAG `1.4.12 Text Spacing` použít bookmarklet, devtools nebo uživatelský stylesheet s ekvivalentem:

```css
* {
  line-height: 1.5 !important;
  letter-spacing: 0.12em !important;
  word-spacing: 0.16em !important;
}

p {
  margin-bottom: 2em !important;
}
```

## Obecná pravidla průchodu

- Testovat čistou instalaci i reprezentativní web s obsahem.
- U každého nálezu uložit URL, krok, očekávané chování, skutečné chování a dotčené WCAG kritérium.
- Pokud je chyba v obsahu autora, označit ji jako author-content issue, ne jako core CMS defect.
- Author-content issue posuzovat podle `author-content-checklist.md`: alt text, obrázek s textem, přepis, titulky, jazyk části, text odkazu, tabulka, barva, vlastní HTML, PDF nebo externí embed.
- Pokud chyba vzniká v core UI nebo default šabloně, založit opravnou položku v `a11y-remediation-backlog.md`.
- Při návrhu nového modulu projít tento protokol ještě před implementací a označit scénáře, které modul rozšiřuje nebo které bude potřeba doplnit.

## Scénáře veřejného webu

1. Otevřít homepage a přes skip link přejít na obsah.
2. Projít hlavní navigaci, vyhledávání, footer widgety a sociální odkazy jen klávesnicí.
3. Ve footeru ověřit navigaci `Pomoc a kontakt`: čtečka má ohlásit nadpis, odkazy Kontakt a Chat mají být ve stejném pořadí na homepage i na jiné veřejné stránce a nemají se zobrazit, pokud jsou oba moduly vypnuté. U aktivní ne-default portable/static šablony ověřit stejný průchod; u ručně psané PHP šablony nebo provozního override patičky zapsat samostatný nález nebo výjimku.
4. Otevřít blog index, článek s osnovou, kategorii, štítek a sérii.
5. Odeslat blogový komentář se správnými i chybnými hodnotami. U chybné captchy, prázdného jména, neplatného e-mailu a prázdného komentáře ověřit souhrnný `role="alert"`, field-level text u konkrétního pole, `aria-invalid`, `aria-describedby` na existující prvek a zachování vyplněných hodnot.
6. Otevřít Form Builder formulář, způsobit chybu, opravit ji a odeslat; u polí pro jméno, e-mail, telefon, URL, firmu, pracovní pozici, datum narození, ulici, PSČ, město, kraj a zemi ověřit, že prohlížeč nebo správce hesel nabídne odpovídající autofill a že pole typu `Adresa webu` se nechová jako poštovní adresa. U prázdného povinného textu, výběru, souhlasu a uploadu, neplatného e-mailu, neplatné URL, nepovolené výběrové hodnoty, chybné přílohy a chybné ověřovací otázky ověřit, že čtečka oznámí field-level text s konkrétním návrhem opravy.
7. Otevřít galerie album a detail fotografie, ověřit alt text, figcaption a metadata.
8. Otevřít media/PDF/audio/video snippet a ověřit názvy iframe/playerů. U přímého videa s WebVTT titulky a audio-description stopou ověřit, že prohlížeč nabízí stopy se správným jazykem a názvem; u audio/video snippetu s `transcript` ověřit dosažitelný odkaz na přepis.
9. Otevřít podcastovou epizodu s vyplněným přepisem a ověřit, že čtečka oznámí sekci `Přepis epizody` jako textovou alternativu audia; u externích video embedů ověřit titulky nebo popsanou odpovědnost autora.
10. Projít reprezentativní článek nebo stránku podle `author-content-checklist.md`: obrázky s alt textem, obrázky s textem, cizojazyčný úsek s `lang`, srozumitelné odkazy, nadpisy, tabulky, barvu, vlastní HTML a externí embed. Stejnou kontrolu jazyka části textu udělat u alespoň jednoho veřejného taxonomického popisu, například blogové kategorie, štítku/série, kategorie vývěsky, typu akce nebo kategorie/série Ke stažení.
11. Otevřít ankety, FAQ feedback, chat, kontakt a newsletter subscribe; jako přihlášený veřejný uživatel ověřit, že kontaktní formulář předvyplní známé jméno a e-mail, ale dovolí hodnoty upravit a po validační chybě je nepřepíše zpět profilem.
12. Otevřít board, downloads, events, places, reservations a food detail; u guest rezervace vyvolat chybnou ověřovací otázku a ověřit field-level text s návrhem opravy u pole captcha. U Food objednávky jako přihlášený veřejný uživatel ověřit předvyplněné jméno, e-mail a telefon, ruční úpravu a zachování upravených hodnot po chybě.
13. Ověřit 404, 429, potvrzení e-mailu, odhlášení newsletteru a maintenance stránku.

## Scénáře administrace

1. Přihlášení, registrace, 2FA, tokenový reset hesla a odhlášení bez myši; registrace a žádost o reset nesmí vyžadovat matematickou CAPTCHA ani jiný kognitivní test. V Mém profilu vyvolat chybu e-mailu, hesla, kontrolního hesla, veřejného autora a TOTP ověřovacího kódu; souhrnný alert má být atomický, dotčená pole mají mít `aria-invalid`, `aria-describedby` na existující nápovědu/chybu a konkrétní návrh opravy.
2. Dashboard: Moje zkratky, fronta ke schválení, poslední aktivita a statistiky.
3. Levá navigace a command centrum přes `Ctrl+K`.
4. Otevřít `Nápověda a podpora` ze spodní části administrační navigace, ověřit stabilní pořadí před odkazy `Web` a `Odhlásit se`, dosažitelnost jen klávesnicí, smysluplné nadpisy sekcí a odkazy na command centrum, profil, kontaktní workflow, nastavení a dokumentaci.
5. Media/content picker: otevření, hledání, změna typu výsledku, vložení a zavření; v čistém HTML editoru vybrat cizojazyčný text, použít helper `Jazyk části textu`, ověřit vložení `<span lang="en">…</span>`, live oznámení a zachování fokusu. Stejný průchod zopakovat v některém taxonomickém editoru, jehož popis se veřejně renderuje jako HTML.
6. Widget dialog: otevření, změna typu widgetu, focus trap, uložení, návrat fokusu. V přehledu widgetů zkusit odebrání aktivního widgetu bez `confirm_widget_delete_<id>`: čtečka má oznámit review názvu, typu, zóny a dopadu na veřejné zobrazení, textový alert a field-level chybu u checkboxu, widget ani audit log se nesmí změnit; s potvrzením se má zobrazit PRG stav `Widget byl odebrán.`.
7. Editor blogového článku, správa blogů, blogových kategorií/štítků, sérií článků, externích odkazů blogu, statické stránky, novinky, FAQ a FAQ kategorií: dlouhá editace, content lock heartbeat, uložení, re-auth po vypršení session a chybové stavy. Při re-auth scénáři rozepsat obsah, nechat session vypršet, ověřit textové live upozornění heartbeat 401, odeslat formulář, potvrdit návratový status na admin loginu, znovu se přihlásit, vrátit se na původní stránku a ověřit nabídku recovery konceptu, obnovení i zahození bez rozbitých `aria-describedby` vazeb. Stejný scénář zopakovat s 2FA pending session, ve dvou panelech a v soukromém okně nebo se zablokovaným localStorage, pokud to prohlížeč dovolí. Ve správě blogů ověřit prázdný název, prázdný/rezervovaný/duplicitní slug a zachování rozepsaných hodnot. U editoru článku ověřit chybějící titulek a text, nepoužitelný slug, cizí kategorii, cizí související článek a cizí sérii; u statické stránky ověřit chyby názvu, slugu, přiřazeného blogu, plánovaného publikování a plánovaného zrušení publikace; u novinky ověřit chybějící titulek/text a nepoužitelný nebo duplicitní slug; u blogových kategorií a štítků ověřit prázdný název a duplicitní slug; u série článků prázdný název; u externího odkazu blogu prázdný název a neplatnou URL; u FAQ ověřit prázdnou otázku/odpověď a neplatný nebo duplicitní slug; u FAQ kategorií ověřit prázdný název, slug bez použitelného znaku, duplicitní slug a příliš dlouhý meta title. Souhrnný `role="alert"` má upozornit na dotčené pole, pole má mít `aria-invalid`, `aria-describedby` na existující nápovědu/chybu a text má radit konkrétní opravu.
8. Form Builder: přidání pole, změna typu pole, chybové stavy, náhled a odpovědi. U editoru formuláře ověřit chyby názvu, slugu, notifikačního e-mailu, potvrzovacího e-mailového pole a webhook URL: souhrnný `role="alert"` má upozornit na dotčené pole, pole má mít `aria-invalid`, `aria-describedby` na existující nápovědu/chybu a text má radit konkrétní opravu. V přehledu odpovědí otevřít CSV export, ověřit, že nejdřív čtečka oznámí review citlivosti odpovědí a aktuálního filtru, bez `confirm_form_submissions_csv_export` se nestáhne soubor ani nevznikne audit log a chyba se oznámí jako atomický alert i field-level chyba u checkboxu; s potvrzením se stáhne CSV attachment. Ve stejném přehledu vybrat jednu nebo více odpovědí a zkusit změnu stavu i trvalé smazání bez `confirm_form_submissions_bulk_action`: čtečka má oznámit textový alert a field-level chybu u checkboxu, odpověď/stav/přílohy/historie ani audit log se nesmí změnit; s potvrzením se akce provede a vrátí PRG stav.
   V přehledu formulářů zkusit smazání celého formuláře bez `confirm_form_delete_<id>`: čtečka má oznámit review veřejné URL, počtu polí, odpovědí, historie odpovědí a dopadu na přílohy, textový alert a field-level chybu u checkboxu; formulář, pole, odpovědi, historie ani audit log se nesmí změnit. S potvrzením se má zobrazit PRG stav `Formulář byl smazán.` a data formuláře se mají odstranit.
   V přehledu odpovědí i detailu jedné odpovědi zkusit individuální smazání bez `confirm_form_submission_delete_<id>`: čtečka má oznámit review reference, stavu, počtu záznamů interní historie a příloh, textový alert a field-level chybu u checkboxu; odpověď, historie, přílohy ani audit log se nesmí změnit. S potvrzením se má zobrazit PRG stav `Odpověď formuláře byla smazána.` a data odpovědi se mají odstranit.
9. Admin tabulky s hromadnými akcemi: media, comments, contact, chat, statistics, audit log. Ve statistikách otevřít kontrolu CSV exportu obsahu a zkusit stažení bez `confirm_statistics_content_csv_export`: čtečka má oznámit review interních metrik, atomický alert a field-level chybu u checkboxu, CSV se nesmí stáhnout ani zapsat do audit logu; s potvrzením se stáhne attachment. V Audit logu otevřít `Zkontrolovat CSV export` nad nefiltrovaným i filtrovaným výpisem a zkusit stažení bez `confirm_audit_log_csv_export`: čtečka má oznámit citlivost provozních záznamů, aktuální filtr, atomický alert a field-level chybu u checkboxu, CSV se nesmí stáhnout ani zapsat do audit logu; s potvrzením se stáhne attachment. U FAQ nebo jiného běžného modulu se sdíleným `bulkActions()` vybrat jednu položku a zkusit hromadné smazání bez `confirm_bulk_delete`: čtečka má oznámit review text a field-level chybu u checkboxu, položka ani audit log se nesmí změnit; s potvrzením se akce provede a vrátí PRG stav.
10. V detailu kontaktní zprávy, chat zprávy a odpovědi Form Builderu zkusit odeslat e-mailovou odpověď bez předmětu nebo bez textu. Ověřit souhrnný `role="alert"`, field-level chybu u předmětu i textu, `aria-invalid`, `aria-describedby` na existující prvky a konkrétní radu doplnit předmět nebo text odpovědi. V detailu odpovědi Form Builderu navíc vyvolat chybu vytvoření GitHub issue a chybu napojení existující issue; ověřit stejný pattern u repozitáře, názvu issue, těla issue a URL existující issue. V newsletter composeru ověřit prázdný předmět a text rozesílky se stejnou vazbou na dotčená pole.
   V přehledu newsletteru i detailu odběratele zkusit individuální smazání bez `confirm_newsletter_subscriber_delete_<id>`: čtečka má oznámit review dopadu, textový alert a field-level chybu u checkboxu, odběratel ani audit log se nesmí změnit; s potvrzením se má zobrazit PRG stav `Odběratel byl smazán.`.
11. Upload a editace média včetně alt textu, licence, kolekce, alb a fotografií. V knihovně médií najít nepoužitý soubor a zkusit individuální smazání bez `confirm_media_delete_<id>`: čtečka má oznámit review souboru/metadat/miniatur, atomický alert a field-level chybu u checkboxu, DB záznam ani fyzický soubor se nesmí odstranit a audit log se nesmí změnit; s potvrzením se médium smaže s PRG stavem. U alba galerie ověřit prázdný název, duplicitní slug, nepoužitelné nadřazené album a neplatnou výchozí licenci; u fotografie galerie ověřit duplicitní slug, neplatnou licenci a neplatné datum pořízení. Souhrnný alert má být atomický, dotčené pole má mít `aria-invalid`, `aria-describedby` na existující nápovědu/chybu a text má radit konkrétní opravu. V přehledu alb vybrat jedno nebo více alb a zkusit ZIP export i smazání bez `confirm_gallery_albums_bulk_action`: čtečka má oznámit review a field-level chybu u checkboxu, ZIP se nesmí stáhnout, album/fotky/revize se nesmí odstranit a audit log se nesmí změnit; s potvrzením se ZIP stáhne nebo smazání projde s PRG stavem.
12. Nastavení webu, nastavení modulů, správa uživatelů, vzhled/šablony, hlavní navigace, přesměrování, Koš, import/export a migrace. U nového uživatelského účtu ověřit neplatný e-mail, krátké heslo, neshodné kontrolní heslo, slug veřejného autora a web autora. U nastavení webu ověřit prázdný název webu, příliš dlouhý veřejný název vývěsky, neplatný GitHub repozitář a nepovolené logo/faviconu. U šablon ověřit neplatnou aktivní šablonu, neplatnou barvu v theme settings, prázdný nebo chybný ZIP import a neplatnou šablonu pro export. U ZIP exportu šablony zkusit stažení bez `confirm_theme_export`: čtečka má oznámit review přenášené konfigurace, atomický alert a field-level chybu u checkboxu; ZIP se nesmí stáhnout ani zapsat do audit logu, s potvrzením se stáhne attachment. U hlavní navigace ověřit externí odkaz bez názvu a s neplatnou URL; u přesměrování ověřit prázdnou/neplatnou starou cestu, prázdnou/neplatnou novou cestu, shodné cesty a duplicitní starou cestu. V Koši ověřit, že řádek trvale mazané položky čtečka oznámí s typem, názvem a datem smazání, že purge bez potvrzovacího checkboxu neprojde, oznámí atomický alert i field-level chybu u konkrétního checkboxu a že potvrzení je zřetelně nevratné. U ruční SQL zálohy databáze ověřit, že review text jasně říká citlivost exportu, stažení bez potvrzovacího checkboxu zůstane na HTML chybě bez downloadu a čtečka oznámí souhrnný alert i field-level chybu u checkboxu; s potvrzením se stáhne SQL attachment. U hlavního JSON exportu ověřit, že obrazovka nejdřív popíše citlivé části exportu, například zprávy, komentáře, odběratele a tokeny odběru; bez `confirm_json_export` se nestáhne soubor, nevznikne audit log a čtečka oznámí alert i field-level chybu u checkboxu; s potvrzením se stáhne JSON attachment. Souhrnný alert má být atomický, dotčené pole má mít `aria-invalid`, `aria-describedby` na existující nápovědu/chybu a odeslané hodnoty se mají po chybě zachovat.
   U přesměrování navíc zkusit smazání jednoho řádku bez `confirm_redirect_delete_<id>`: čtečka má oznámit review staré/nové cesty, textový alert a field-level chybu u checkboxu, přesměrování ani audit log se nesmí změnit; s potvrzením se má zobrazit PRG stav `Přesměrování smazáno.`.
   U správy uživatelů navíc zkusit smazání běžného účtu bez `confirm_user_delete_<id>`: čtečka má oznámit review e-mailu/role/přiřazených blogů a dopad na osobní administrační zkratky, textový alert a field-level chybu u checkboxu, účet, zkratky ani audit log se nesmí změnit; s potvrzením se má zobrazit PRG stav `Uživatelský účet byl smazán.` a osobní zkratky účtu mají být uklizené.

13. U míst a podcastů zadat neplatnou URL do volitelného URL pole a ověřit, že field-level chyba vysvětluje povolený http/https nebo doménový tvar a možnost pole vynechat. U místa navíc ověřit chybějící název, nepoužitelný nebo duplicitní slug a souřadnice mimo povolený rozsah. U podcastového pořadu navíc ověřit chybějící název, nepoužitelný nebo duplicitní slug, RSS limit a cover obrázek. U podcastové epizody ověřit chybějící název, nepoužitelný nebo duplicitní slug, audio upload, externí audio URL, obrázek epizody a plánované publikování. V editorech kapitol, osob a platforem vyvolat neplatný čas kapitoly, prázdný název, neplatnou URL a duplicitní začátek nebo platformu; s NVDA a jen klávesnicí potvrdit jediné oznámení atomického alertu, `aria-invalid`, vazbu na existující field-level text, zachování ostatních hodnot a srozumitelný konkrétní návrh opravy.
14. U administračních e-mailových polí zadat neúplnou hodnotu bez domény nebo zavináče a ověřit, že chyba radí úplnou adresu ve tvaru `jmeno@example.cz`; u volitelných polí možnost pole vynechat a u přihlašovacího e-mailu jedinečnost adresy.
15. U běžných datumových polí ve vývěsce, jídelních lístcích, událostech a downloadech zadat neplatné datum a ověřit, že chyba radí kalendářní datum, prázdné volitelné pole nebo opravu pořadí od/do; u data vydání ověřit i field-level chybu a `aria-describedby`. U položky vývěsky ověřit prázdný nadpis a datum vyvěšení, nepoužitelný slug a neexistující kategorii; u jídelního/nápojového lístku ověřit prázdný název, nepoužitelný slug a neplatné pořadí dat. U události ověřit chybějící název a datum začátku, nepoužitelný nebo duplicitní slug, konec před začátkem, neplatnou registrační URL, neexistující typ akce, neexistující místo a neplatné opakování.
16. U položek ke stažení ověřit prázdný název, nepoužitelný nebo duplicitní slug, neexistující sérii, chybějící zdroj, neplatný externí odkaz, neplatnou domovskou stránku projektu, neplatný SHA-256 checksum a nepovolený upload. Každá chyba musí mít srozumitelný form-level alert i field-level text, `aria-describedby` na existující prvek, zachovanou hodnotu pole a konkrétní opravu: název položky, jedinečný slug, existující série nebo žádná série, lokální soubor nebo externí URL, http/https nebo doména bez schématu, prázdné volitelné URL pole, případně přesně 64 znaků `0-9` a `a-f`. V administraci kategorií a sérií ke stažení navíc ověřit prázdný název, nepoužitelný nebo duplicitní slug a příliš dlouhý meta title kategorie; souhrnný alert má být atomický a čtečka má oznámit konkrétní field-level návrh opravy. U existující kategorie a série s navázanými položkami zkusit smazání bez potvrzovacího checkboxu: čtečka má oznámit review počtu navázaných položek, textový alert a field-level chybu u checkboxu, vazby položek ani audit log se nesmí změnit; s potvrzením se kategorie/série smaže a položky zůstanou zachované bez dané vazby. Stejný ruční průchod zopakovat u kategorií vývěsky, typů akcí, témat kontaktu/chatu a rezervačních kategorií/míst: zkontrolovat povinný název, slugové chyby, existující `aria-describedby` cíle a zachování rozepsaných hodnot po chybě. U kategorie vývěsky navíc zkusit smazání bez `confirm_board_category_delete_<id>`: čtečka má oznámit review počtu navázaných položek a odběrů, textový alert a field-level chybu u checkboxu, kategorie, vazba položky, vazba odběru ani audit log se nesmí změnit; s potvrzením se kategorie smaže a položky/odběry zůstanou zachované bez dané vazby. U typu akce zkusit smazání bez `confirm_event_type_delete_<id>`: čtečka má oznámit review počtu navázaných událostí, textový alert a field-level chybu u checkboxu, typ, vazba události ani audit log se nesmí změnit; s potvrzením se typ smaže a události zůstanou zachované bez typu. U tématu kontaktu a chatu navíc zkusit smazání bez `confirm_contact_topic_delete_<id>` nebo `confirm_chat_topic_delete_<id>`: čtečka má oznámit review dopadu na veřejný formulář/chat a existující zprávy, textový alert a field-level chybu u checkboxu, téma, vazba zprávy ani audit log se nesmí změnit; s potvrzením se téma smaže a existující zprávy zůstanou zachované bez vazby. U rezervační kategorie a místa zkusit smazání bez `confirm_res_category_delete_<id>` nebo `confirm_res_location_delete_<id>`: čtečka má oznámit review počtu navázaných zdrojů, textový alert a field-level chybu u checkboxu, číselník, vazba zdroje ani audit log se nesmí změnit; s potvrzením se číselník smaže a zdroje/rezervace zůstanou zachované bez dané vazby. U blogové kategorie, štítku a série článků zkusit smazání bez `confirm_blog_category_delete_<id>`, `confirm_blog_tag_delete_<id>` nebo `confirm_blog_series_delete_<id>`: čtečka má oznámit review počtu navázaných článků a u kategorie i podkategorií, textový alert a field-level chybu u checkboxu, taxonomie, články, podkategorie, štítkové/sériové vazby ani audit log se nesmí změnit; s potvrzením se vazby odpojí a obsah zůstane zachovaný bez dané taxonomie nebo série. U FAQ kategorie zkusit smazání bez `confirm_faq_category_delete_<id>`: čtečka má oznámit review počtu navázaných otázek a podkategorií, textový alert a field-level chybu u checkboxu, kategorie, otázky, podkategorie ani audit log se nesmí změnit; s potvrzením se otázky odpojí od kategorie, podkategorie se přesunou na kořen a obsah zůstane zachovaný.
17. U obrazových uploadů v článku, vývěsce, události, místě a downloadu zkusit nepovolený formát včetně SVG. Ověřit form-level alert i field-level text, zachované hodnoty, existující `aria-describedby`, radu JPEG/PNG/GIF/WebP, výslovné odmítnutí SVG a možnost volitelné pole nechat prázdné; u download náhledu ověřit i novou lokální chybu u pole.
18. U přílohy vývěsky a audio souboru podcastové epizody zkusit nepovolený formát. Ověřit form-level alert i field-level text, existující `aria-describedby`, radu povolených formátů, zachované hodnoty a možnost nechat volitelné pole prázdné; u podcastu ověřit i srozumitelnou alternativu externího audio odkazu.
19. V knihovně médií odeslat prázdný hromadný upload, SVG nebo nepodporovaný soubor a ověřit, že po redirectu existuje form-level alert i field-level chyba u `media_files` s `aria-invalid` a `aria-describedby` na existující text. V detailu média zkusit náhradu souborem mimo MIME rodinu nebo s jinou příponou veřejného souboru a ověřit stejný pattern u `replacement_file`.
20. U importů odeslat prázdný JSON import, prázdný WordPress WXR import, prázdný eStránky XML import a u downloaderu fotek kombinaci prázdného XML/neplatné URL. Ověřit `role="alert"`, field-level text u konkrétního pole, existující `aria-describedby`, zachování zadané URL u downloaderu a srozumitelnou opravu: exportní soubor, platné UTF-8 nebo http/https/doménový tvar URL. U JSON importu, finálního WordPress náhledu, eStránky XML importu a downloaderu fotografií také ověřit, že validní zdroj bez potvrzovacího checkboxu neprojde, čtečka oznámí konkrétní field-level chybu a review text jasně říká, jaký zápis dat nebo souborů se má spustit.
21. U plánování publikace, ukončení publikace, rezervační dostupnosti a časových rozsahů zadat neplatnou datum/čas hodnotu a ověřit, že chyba radí použít ovladač datum/čas, nechat volitelné pole prázdné, odstranit prázdný řádek nebo opravit pořadí začátku a konce. V editoru rezervačního zdroje navíc ověřit chybějící název, nepoužitelný slug, kapacitu a předstih připomínky; každá chyba musí být navázaná na dotčené pole a radit konkrétní opravu. V ručním vytvoření rezervace ověřit chybějící zdroj, chybějícího uživatele u registrované rezervace, chybějící jméno hosta, prázdné datum/čas, konec před začátkem a konflikt s existující rezervací; každá chyba musí mít field-level text u dotčených polí a zachované odeslané hodnoty. V detailu rezervace vyzkoušet schválení, zamítnutí, zrušení, dokončení a no-show bez potvrzovacího checkboxu: čtečka má oznámit textový alert, konkrétní checkbox má mít field-level chybu a stav ani historie rezervace se nesmí změnit; s potvrzením se změna zapíše a vrátí PRG stav. V editoru ankety ověřit chybějící otázku a dvě možnosti, více než deset možností, limit vícevýběrové ankety mimo rozsah, odebrání možnosti s uloženými hlasy a nepoužitelný slug. U ankety s hlasy otevřít kontrolu CSV exportu výsledků a zkusit stažení bez `confirm_poll_results_csv_export`: čtečka má oznámit review agregovaných výsledků, atomický alert a field-level chybu u checkboxu; CSV se nesmí stáhnout ani zapsat do audit logu, s potvrzením se stáhne attachment bez raw hashů. Vyvolat také klientskou chybu hromadného generátoru slotů a přidání blokovaného dne bez data; ověřit, že se nezobrazí prohlížečový alert, ale inline `role="alert"` text s návrhem opravy a focus zůstane na poli, které je potřeba opravit.
22. U řaditelných administračních seznamů, například blogů, použít tlačítka Nahoru/Dolů, klávesovou zkratku Ctrl+šipka i drag/drop. Ověřit, že úspěšné uložení pořadí vytvoří textovou status hlášku, simulované selhání sítě nebo serveru vytvoří inline `role="alert"` s retry textem a nikde se nezobrazí prohlížečový alert.
23. Při 320 px šířce a 400 % zoomu ověřit, že admin navigace nepřekrývá hlavní obsah, tabulky rolují jen ve své ose, dlouhé fieldsety nevyvolávají horizontální scroll hlavního obsahu, action rows zůstávají ovladatelné, theme katalogy, checkbox skupiny a souhrnné karty padají do jednoho sloupce, flex řádky s daty/volbami se nesrážejí přes sebe a focus není schovaný mimo viditelný scroll; po 320px ověření hlavních hustých tabulek a reprezentativních dlouhých formulářů pokračovat hlavně přes 400 % zoom, custom moduly, sticky/anchor skoky a keyboard-only/NVDA kombinace.

## Scénáře pro nové moduly

Před větší implementací modulu zapsat, zda přidává nebo mění:

- veřejný výpis, detail, filtr, widget, sitemapu, RSS/ICS/CSV/PDF export nebo tokenový endpoint;
- administrační přehled, hromadné akce, dialog, picker, upload, rich text editor nebo dlouhý formulář;
- autorem dodávaný obsah, média, embedy, přílohy, alt texty, titulky, transcript nebo vlastní HTML;
- captcha/auth flow, časový limit, rate-limit, automatické přesměrování, live region nebo stavové hlášky;
- tabulky, drag/drop, ovládání pouze myší, nové barvy, nové ikony nebo význam nesený vizuálním stylem.

Každá kladná odpověď musí mít ruční testovací scénář, automatizovatelný guardrail nebo záznam v `a11y-remediation-backlog.md`.

## Kontrolní checklist

- Viditelný focus je stále vidět a není zakrytý sticky prvkem.
- Pořadí tabulátoru odpovídá vizuální a logické hierarchii.
- Žádný dialog neuzamkne klávesnici bez možnosti zavřít ho.
- Čtečka hlásí název, roli, stav a hodnotu ovládacích prvků.
- Stavové hlášky se oznámí jednou, neruší při každém napsaném znaku a klientské validační chyby nepoužívají prohlížečový alert tam, kde lze zobrazit textový inline alert.
- Každé pole má srozumitelný label a chyba je spojena s polem.
- Chybové hlášky, kde je známý opravný krok, neříkají jen „chybná hodnota“, ale stručně radí, jak pokračovat.
- Ručně vložený obsah prošel `author-content-checklist.md` a každý nález je označený jako core CMS defect, theme defect nebo author-content issue.
- Cizojazyčný úsek v author contentu i veřejně renderovaném taxonomickém popisu má odpovídající `lang`; helper `Jazyk části textu` je použitelný bez myši a nevytváří neexistující ARIA vazby.
- Autentizační flow nevyžaduje řešení hádanek, opisování CAPTCHA ani jiný kognitivní test bez alternativy.
- Správce hesel nabídne vyplnění veřejného i administračního přihlášení a rozpozná vytvoření nebo změnu hesla bez ručního přepisování.
- Autofill u veřejného kontaktu, objednávkové poptávky, guest rezervace, chatu, blogového komentáře a Form Builder formuláře nenabízí zavádějící hodnoty a u běžných osobních polí rozpozná jméno, e-mail, telefon, URL, organizaci, pracovní pozici, datum narození a adresní údaje.
- Veřejný kontakt, Food objednávka, veřejný chat, odpověď v chatu a blogový komentář nepřinutí přihlášeného veřejného uživatele znovu zadat kontaktní údaje, které CMS bezpečně zná z profilu, a po ruční úpravě nezahodí zadanou hodnotu při validační chybě.
- Trvalé smazání v Koši má před odesláním čitelný review kontext, samostatné potvrzení nevratnosti, field-level chybu u konkrétní položky po chybějícím potvrzení a serverově odmítá nepotvrzený purge bez audit logu.
- Mazání jednotlivého přesměrování má před odesláním čitelný review staré a nové cesty, samostatné potvrzení dopadu na veřejné URL a serverově odmítá nepotvrzené smazání bez změny dat nebo audit logu.
- Mazání uživatelského účtu má před odesláním čitelný review dopadu na přístup do CMS a osobní administrační zkratky, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny účtu, zkratek nebo audit logu.
- Mazání nepoužitého média má před odesláním čitelný review souboru, metadat a odvozených miniatur, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny DB záznamu, fyzického souboru nebo audit logu.
- Odebrání widgetu má před odesláním čitelný review názvu, typu, zóny a dopadu na veřejné zobrazení, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny widgetu nebo audit logu.
- Mazání jednotlivého odběratele newsletteru má před odesláním čitelný review dopadu na aktivní odběr a historii rozesílek, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny odběratele nebo audit logu.
- Mazání kategorií a sérií Ke stažení má před odesláním čitelný review počtu navázaných položek, u série i aktuálních verzí, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny vazeb, taxonomie nebo audit logu.
- Mazání kategorie vývěsky nebo typu akce má před odesláním čitelný review počtu navázaných položek/odběrů nebo událostí, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny taxonomie, vazeb nebo audit logu.
- Mazání tématu kontaktu nebo chatu má před odesláním čitelný review dopadu na veřejný kontaktní formulář nebo chat a existující zprávy, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny tématu, vazeb zpráv nebo audit logu.
- Mazání rezervační kategorie nebo místa má před odesláním čitelný review počtu navázaných rezervačních zdrojů, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny číselníku, vazeb zdrojů nebo audit logu.
- Mazání FAQ kategorie má před odesláním čitelný review počtu navázaných otázek a podkategorií, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez změny kategorie, otázek, podkategorií nebo audit logu.
- Mazání celého blogu má před odesláním čitelný review počtu článků, kategorií, štítků, sérií a týmových přiřazení, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez přesunu obsahu, odstranění sérií, zrušení týmu, smazání blogu nebo audit logu.
- Mazání rezervačního zdroje má před odesláním čitelný review počtu budoucích nezrušených rezervací, vazeb na místa, pravidel otevírací doby, slotů a blokovaných dnů, samostatné potvrzení a serverově odmítá nepotvrzené smazání bez zrušení rezervací, odstranění dostupnosti, smazání zdroje nebo audit logu.
- Ruční SQL záloha má před stažením čitelný review citlivosti exportu, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez odeslání souboru.
- Hlavní JSON export CMS má před stažením čitelný review citlivých dat, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez attachmentu nebo audit logu.
- CSV export odpovědí Form Builderu má před stažením čitelný review citlivosti odpovědí a aktuálního filtru, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez attachmentu nebo audit logu.
- CSV export výsledků anket má před stažením čitelný review tématu a agregovaných výsledků, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez attachmentu nebo audit logu.
- CSV export statistik obsahu má před stažením čitelný review interních metrik výkonu obsahu, období, filtru a počtu řádků, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez attachmentu nebo audit logu.
- Hromadné akce nad odpověďmi Form Builderu mají před změnou stavu nebo trvalým smazáním čitelný review vybraných odpovědí a zvolené akce, samostatné potvrzení a serverově odmítají nepotvrzený požadavek bez změny dat nebo audit logu.
- Mazání celého Form Builder formuláře má před odesláním čitelný review veřejné URL, počtu polí, odpovědí, historie odpovědí a dopadu na přílohy, samostatné potvrzení a serverově odmítá nepotvrzený požadavek bez změny formuláře, polí, odpovědí, historie nebo audit logu.
- Hromadné smazání a ZIP export alb galerie mají před odesláním čitelný review vybraných alb a zvolené akce, samostatné potvrzení a serverově odmítají nepotvrzený požadavek bez smazání, attachmentu nebo audit logu.
- ZIP export šablony má před stažením čitelný review přenášené konfigurace a statických assetů, samostatné potvrzení oprávnění a serverově odmítá nepotvrzený download bez attachmentu nebo audit logu.
- Sdílené hromadné smazání přes `bulkActions()` má čitelný review dopadu, checkbox `confirm_bulk_delete`, field-level chybu při chybějícím potvrzení a serverově odmítá nepotvrzený požadavek bez cleanupu, smazání nebo audit logu.
- TOTP pole je použitelné jen klávesnicí, rozpoznatelné jako jednorázový kód a jeho chyba se oznámí jako jeden alert.
- Tokenový reset hesla jde dokončit bez znovuzadávání údajů, které už CMS zná, a chybový token má srozumitelnou textovou zpětnou vazbu.
- Při vypršení admin session dlouhý formulář neztratí rozepsaný obsah: heartbeat oznámí vypršení přihlášení, login oznámí návrat na původní stránku a autosave recovery koncept jde obnovit nebo vědomě zahodit.
- Admin nápověda je dostupná ze stabilního místa v navigaci, má smysluplné nadpisy a nevyžaduje hledání odlišné podpory na každé obrazovce.
- Veřejný footer default i portable/static theme variant má stabilní `Pomoc a kontakt` navigaci; ručně psaná PHP šablona nebo provozní override patičky nezavádí odlišný, hůře dostupný help/contact vzor bez samostatné ACR výjimky.
- Tabulky mají caption nebo pojmenování přes skutečný nadpis.
- Odkazy otevírané v novém okně to oznamují ve svém přístupném názvu.
- Při zoomu 200 % a 400 % nedochází ke ztrátě obsahu nebo vodorovnému scrollu mimo povolené výjimky.
- Při text spacing override se text neztrácí, nepřekrývá sousední obsah, není zkrácený ellipsis/line clampem a ovládací prvky zůstávají použitelné.
- Na 320 px jsou primární akce dosažitelné a použitelné.
- Runtime audit `admin_mobile_reflow_guardrails` prochází pro admin mobilní baseline, datové tabulky, media grid, Form Builder gridy, statistics/theme/checkbox/summary gridy, forms overview tabulku, husté moduly comments/contact/chat/reservations/food/downloads/gallery/podcast, dlouhé form controls a rezervační otevírací dobu, widget řazení, cache-busting admin stylesheetu, minimální rozměr běžných tlačítek, checkbox/radio ovladačů, řadicích ovladačů, sekundárních action odkazů i přímých action odkazů v odstavcích.
- Datové tabulky mohou mít horizontální scroll, ale nesmí rozšířit celou stránku spolu se sidebar navigací ani schovat focusovaný prvek bez možnosti ho dorolovat.
- Runtime audit `contrast_focus_guardrails` prochází pro default/admin/login text, skip link, focus, hranice inputů, tlačítek a informačních panelů, info panely, inline štítky, forced-colors fallback, focus/anchor scroll-margin a disabled ovládací prvky bez opacity dimmingu.
- Runtime audit `text_spacing_guardrails` prochází pro core CSS bez záporného `letter-spacing`, textového ořezu a text-spacing `!important` zámků.
- Ručně změřené hover, active, disabled, ikony, progress bary a custom theme settings splňují AA; u textu alespoň 4.5:1, u focusu a hranic ovládacích prvků alespoň 3:1.
- Focus prstenec je při klávesnici viditelný na světlém i tmavém pozadí a nezaniká pod outline/box-shadow jiného prvku.

## Zápis výsledků

Pro každý nález použít formát:

```text
ID:
Datum:
Testovací prostředí:
URL / obrazovka:
Kroky:
Očekáváno:
Skutečnost:
WCAG:
Priorita:
Poznámka:
```

Po každém ručním kole aktualizovat `wcag-22-aa-conformance.md` a podle potřeby `acr-vpat-wcag-draft.md`.
