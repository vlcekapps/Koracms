# Accessibility Remediation Backlog

Tento backlog navazuje na `wcag-22-aa-conformance.md`. Neobsahuje všechny nápady, ale nejdůležitější rizika, která mohou změnit stav WCAG/ACR řádků z `Partially Supports` na `Supports`.

## Vysoká priorita

| Oblast | Kritéria | Riziko | Doporučený další krok |
|---|---|---|---|
| Aktuálně bez otevřeného kritického nálezu | — | Prioritní 320 px reflow průchod hlavních admin obrazovek byl provedený 2026-07-04 a potvrzený browser měřením. | Další kritické nálezy sem přesunout až po ručním průchodu zbylých modulů nebo po regresi. |

## Střední priorita

| Oblast | Kritéria | Riziko | Doporučený další krok |
|---|---|---|---|
| Média a titulky | 1.2.1 až 1.2.5 | Podcastové epizody mají modelované přepisy, audio/video snippety umí odkázat přepis, přímý video snippet umí WebVTT titulky i audio-description stopu a redakční checklist popisuje kvalitu titulků/přepisů/popisu. Zbývá ruční ověření timing/kvality, browser chování a externí platformy. | Ručně ověřit screen-reader/browser chování, kvalitu/timing ukázkových titulků, přepisů a audio-description stop a dostupnost popisu u externích video platforem. |
| Kontrast custom/hover/ikonových stavů | 1.4.3, 1.4.11, 2.4.7 | Runtime audit měří default/admin/login text, focus, input/button/info-panel border tokeny, info panely, inline štítky, forced-colors fallback a disabled ovládací prvky bez opacity dimmingu. Nehlídá ale všechny custom theme settings, hover stavy, ikony a progress bary v reálném prohlížeči. | Ručně změřit default i aktivní theme varianty ve Firefox/Chrome/forced-colors a při nálezu doplnit konkrétní CSS token nebo nový auditní pár. |
| 400% reflow a custom modulové varianty | 1.4.10, 2.4.11, 2.5.8 | Browser průchody 2026-07-04 pokryly hlavní husté admin tabulky, importy, content picker a reprezentativní dlouhé formuláře při 320 px; runtime od 2026-07-06 navíc hlídá focus/anchor scroll-margin, theme/checkbox/summary gridy, úzké flex řádky, checkbox/radio targety a 28px řadicí cíle. Ruční NVDA průchod 2026-07-09 potvrdil bez nahlášené regrese dlouhé formuláře, nastavení, widgety, statické stránky/blogové stránky, statistiky, admin hledání včetně command palety, focus order a osobní zkratky. Zbývají vizuální 400 % zoom, méně časté custom moduly, sticky/anchor kombinace, kontrast a ikonové/hover stavy. | Rozšířit ruční průchod s vidícím testerem na 400 % zoom, custom module admin obrazovky, sticky/anchor skoky a vizuální měření ikonových, hover a focus stavů. |
| Specializovaný input purpose | 1.3.5 | Auth flow, kontakt, food objednávky, guest rezervace, veřejný chat, odpovědi v chatu, blogové komentáře a Form Builder pole pro jméno, kontakt, organizaci, pracovní pozici, datum narození a jasně pojmenovanou adresu mají automatizované guardraily. Zbývá ručně ověřit reálné chování autofillu a doplnit mapování pro případná platební nebo jiná specializovaná custom pole, až je CMS začne sbírat. | Ručně ověřit autofill ve Firefox/Chrome pro reprezentativní Form Builder formulář s adresou a veřejné chat/blog kontaktní formuláře; při přidání platebních polí doplnit explicitní `autocomplete` tokeny, HTTP render a conformance dokumentaci. |
| Text spacing ruční průchod | 1.4.12 | Core CSS má automatizovaný guardrail proti známým blokátorům text spacingu, ale zatím chybí prohlížečové ověření se skutečným text-spacing override. | Projít veřejné a admin šablony s text spacing bookmarkletem nebo ekvivalentním CSS testem ve Firefox/Chrome. |
| Blog modul ruční průchod | 1.4.10, 2.1.1, 2.4.11, 3.3.3 | Blog má modulovou ACR přílohu a automatizované guardraily pro hlavní veřejné/admin workflow včetně komentářových field-level chyb. Ruční NVDA průchod blogů, statických stránek, blogových stránek, dlouhých formulářů a související administrace byl potvrzený 2026-07-09 bez nahlášené regrese. Zbývá vizuální 400% zoom/text-spacing/custom theme průchod a oddělené posouzení reálného author contentu. | Vizuálně projít scénáře v `docs/accessibility/modules/blog.md` při 400 % zoomu a text-spacing override, potvrdit TOC kotvy/sticky prvky a projít reprezentativní články podle author-content checklistu. |
| Session timeout a re-auth | 2.2.1 | Admin autosave, content-lock 401 a login návratový status už mají automatizované guardraily proti ztrátě rozepsaného obsahu při vypršení session. Ruční NVDA průchod dlouhých administračních formulářů byl potvrzený 2026-07-09 bez nahlášené regrese. Zbývá ověřit skutečný timeout v prohlížeči, 2FA pending session, více otevřených panelů a stav bez dostupného nebo zaplněného localStorage. | Ručně projít reálné vypršení session, návrat přes login, obnovení recovery konceptu, discard flow, 2FA pending stav, více panelů a storage edge cases. |
| Error prevention pro kritické akce | 3.3.4 | Koš má automatizovaný review-and-confirm guardrail pro trvalé smazání: soft-deleted položka zobrazuje typ, název a datum smazání, purge vyžaduje potvrzovací checkbox a server odmítne nepotvrzený požadavek. Změna role existujícího uživatelského účtu má samostatný review aktuální/nové role a server odmítne nepotvrzenou změnu. Individuální mazání uživatelského účtu má review dopadu na přístup a osobní zkratky, checkbox `confirm_user_delete_<id>` a server odmítne nepotvrzený POST bez smazání účtu, zkratky nebo audit logu. Individuální mazání nepoužitého média má review souboru, metadat a odvozených miniatur, checkbox `confirm_media_delete_<id>` a server odmítne nepotvrzený POST bez změny DB, fyzického souboru nebo audit logu. Odebrání widgetu má review názvu, typu, zóny a dopadu na veřejné zobrazení, checkbox `confirm_widget_delete_<id>` a server odmítne nepotvrzený POST bez smazání widgetu nebo audit logu. Newsletter rozesílka má review počtu příjemců a server nepotvrzené odeslání odmítne bez zápisu do historie. Hromadné akce nad odběrateli newsletteru mají review checkbox a server odmítne nepotvrzené potvrzení, znovuodeslání i mazání. JSON import, finální WordPress WXR import, XML import z eStránek a dávkové stahování fotografií z eStránek mají potvrzení kontroly zdroje a dopadu před zápisem dat nebo souborů. Stavové změny rezervací v detailu rezervace mají review text, action-specific checkbox a serverové odmítnutí nepotvrzeného požadavku před změnou stavu, historií nebo notifikací. Ruční SQL záloha databáze, hlavní JSON export CMS, CSV export odpovědí Form Builderu, CSV export výsledků anket, CSV export statistik obsahu a ZIP export šablony mají review citlivosti nebo dopadu exportu, potvrzovací checkbox a server odmítne nepotvrzené stažení bez attachmentu i audit logu. Hromadné akce nad odpověďmi Form Builderu mají review vybraných odpovědí a zvolené akce, potvrzovací checkbox a server odmítne nepotvrzenou změnu stavu nebo trvalé smazání bez změny dat a audit logu. Hromadné smazání alb galerie a ZIP export vybraných alb mají společný review checkbox a server odmítne nepotvrzené smazání bez změny dat i nepotvrzený ZIP export bez attachmentu a audit logu. Sdílené hromadné mazání přes `bulkActions()` má generic review checkbox `confirm_bulk_delete` a server odmítne nepotvrzené `delete` akce běžných modulů před cleanupem, smazáním i audit logem. Mazání kategorií a sérií Ke stažení má řádkový review navázaných položek a aktuálních verzí, checkbox `confirm_download_category_delete_<id>`/`confirm_download_series_delete_<id>` a server odmítne nepotvrzený POST bez zrušení vazeb, smazání taxonomie nebo audit logu. Mazání témat kontaktu a chatu má řádkový review dopadu na veřejný kontaktní formulář nebo chat a existující zprávy, checkbox `confirm_contact_topic_delete_<id>`/`confirm_chat_topic_delete_<id>` a server odmítne nepotvrzený POST bez změny vazeb zpráv, smazání tématu nebo audit logu. Mazání rezervačních kategorií a míst má řádkový review počtu navázaných zdrojů, checkbox `confirm_res_category_delete_<id>`/`confirm_res_location_delete_<id>` a server odmítne nepotvrzený POST bez změny vazeb zdrojů, smazání číselníku nebo audit logu. Mazání blogových kategorií, štítků a sérií článků má řádkový review počtu navázaných článků, u kategorií i podkategorií, checkbox `confirm_blog_category_delete_<id>`/`confirm_blog_tag_delete_<id>`/`confirm_blog_series_delete_<id>` a server odmítne nepotvrzený POST bez změny vazeb, smazání taxonomie nebo audit logu. Mazání FAQ kategorií má řádkový review počtu navázaných otázek a podkategorií, checkbox `confirm_faq_category_delete_<id>` a server odmítne nepotvrzený POST bez změny vazeb, přesunu podkategorií, smazání kategorie nebo audit logu. Newsletter workflow má ruční průchod potvrzený 2026-07-09. Zbývá produktově zmapovat další právně/datově kritické akce mimo koš, role, individuální mazání uživatelů, média, odebrání widgetů, importy, stavové změny rezervací, SQL zálohu, hlavní JSON export CMS, CSV export odpovědí Form Builderu, CSV export výsledků anket, CSV export statistik obsahu, ZIP export šablony, hromadné akce odpovědí Form Builderu, hromadné akce galerie a sdílené generic bulk delete flow. | Projít další citlivé ZIP a specializované exporty mimo galerii, šablony a statistiky obsahu, případné další CSV exporty s osobními údaji, zbývající individuální destruktivní akce mimo koš, uživatele, přesměrování, média, widgety, taxonomie Ke stažení, témata kontaktu/chatu, rezervační kategorie/místa, blogové kategorie/štítky/série článků, FAQ kategorie a další externě dopadající operace; u každé určit, zda stačí potvrzení, je potřeba review krok, undo nebo další auditní stopa. |
| Error suggestions | 3.3.3 | Základní veřejné chyby matematického ověření včetně guest rezervací, veřejná custom pole Form Builderu, vybrané administrační URL chyby, e-mailové chyby, běžné datumové chyby, plánovací datum/čas chyby včetně rezervačních zdrojů, klientské chyby generátoru slotů/blokovaného dne, selhání sdíleného AJAX řazení, prázdné e-mailové odpovědi v kontaktním/chat/Form Builder inboxu, chyby GitHub issue bridge ve Form Builder detailu, hlavní validační chyby editoru Form Builderu, hlavní chyby editoru statických stránek, hlavní chyby editoru FAQ, hlavní chyby editoru FAQ kategorií, hlavní chyby blogových kategorií/štítků, hlavní chyby kategorií/sérií ke stažení, kategorie vývěsky, typy akcí, témata kontaktu/chatu, rezervační kategorie/místa, série článků, externí odkazy blogu, základní chyby rezervačních zdrojů, podcastové pořady, newsletter composer, hlavní navigační externí odkazy, přesměrování, alba galerie, fotografie galerie, ruční vytvoření rezervace, hlavní editor článku, správa blogů, položky vývěsky, jídelní/nápojové lístky, ankety, profil správce, správa uživatelských účtů, nastavení webu a správa šablon už radí konkrétní opravu, ale méně časté administrační a modulové validační chyby mohou pořád jen označit hodnotu za chybnou. | Pokračovat copy passem nad zbývajícími administračními, modulovými a méně častými chybami formulářů a doplnit konkrétní návrhy. |
| Redundant entry | 3.3.7 | Guardrail pro hlavní veřejná opakovaná kontaktní flow je hotový: veřejný profil se používá přes `currentUserContactDefaults()` pro jméno, e-mail a telefon; HTTP integrace ověřuje render předvyplnění v kontaktu/Food, veřejném chatu, odpovědi v chatu a blogovém komentáři i uložení veřejné rezervace bez opakované kontaktní výzvy. Riziko zůstává u méně častých admin/custom flow a situací, kde by předvyplnění nebo vynechání opakovaného zadání mohlo být matoucí na sdíleném zařízení. | Zmapovat zbývající administrační a custom flow, ručně projít auth/objednávky/custom formuláře a okrajové rezervační scénáře a doplnit další předvyplnění nebo výjimku tam, kde je to bezpečné a užitečné. |

Poznámka 2026-07-06: individuální mazání přesměrování už není součástí otevřeného `3.3.4` rizika. Má automatizovaný POST review-and-confirm guardrail, serverové odmítnutí bez potvrzení a HTTP důkaz; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu a chybového stavu.

Poznámka 2026-07-06: individuální mazání uživatelského účtu už není součástí otevřeného základního `3.3.4` rizika. Má automatizovaný řádkový review dopadu na přístup a osobní zkratky, serverové odmítnutí bez `confirm_user_delete_<id>` a HTTP důkaz zachování účtu/zkratky/logu i potvrzeného cleanupu; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-06: individuální mazání nepoužitého média už není součástí otevřeného základního `3.3.4` rizika. Má automatizovaný review souboru, metadat a odvozených miniatur, serverové odmítnutí bez `confirm_media_delete_<id>` a HTTP důkaz zachování DB záznamu, fyzického souboru i audit logu; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: odebrání widgetu už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních widgetových akcí. Má řádkový review názvu, typu, zóny a dopadu na veřejné zobrazení, serverové odmítnutí bez `confirm_widget_delete_<id>` a HTTP důkaz zachování widgetu i audit logu bez potvrzení. Ruční NVDA průchod odstraňování a přeskupování widgetů byl potvrzený 2026-07-09 bez nahlášené regrese; opakovat při změnách widget managementu.

Poznámka 2026-07-09: trvalé smazání v Koši už má kromě serverového potvrzení také item-specific field-level chybový stav. Nepotvrzený purge se vrací na konkrétní položku, checkbox `confirm_permanent_delete` dostane `aria-invalid` a vazbu na review text i lokální chybu a HTTP důkaz hlídá zachování položky bez audit logu `trash_purge`; zůstává jen pozdější ruční NVDA/keyboard-only ověření v delším koši.

Poznámka 2026-07-08: CSV export statistik obsahu už není součástí otevřeného základního `3.3.4` rizika. Má samostatný review interních metrik výkonu obsahu, období, filtru modulu a počtu řádků, serverové odmítnutí bez `confirm_statistics_content_csv_export`, žádný attachment ani audit log bez potvrzení a HTTP důkaz potvrzeného bezpečného downloadu bez raw IP hashů, user-agentů nebo referrer tokenů. Ruční NVDA průchod statistik byl potvrzený 2026-07-09 bez nahlášené regrese; opakovat při změnách statistik nebo jejich exportních review stavů.

Poznámka 2026-07-09: CSV export audit logu už není součástí otevřeného základního `3.3.4` rizika specializovaných exportů. Má filter-aware review citlivosti provozních a bezpečnostních záznamů, serverové odmítnutí bez `confirm_audit_log_csv_export`, žádný attachment ani audit log bez potvrzení a HTTP důkaz potvrzeného bezpečného downloadu přes sdílené no-store/noindex/nosniff hlavičky. Ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu zůstává součástí pozdějšího obecného průchodu citlivých exportů.

Poznámka 2026-07-08: mazání kategorií a sérií Ke stažení už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má řádkový review počtu navázaných položek, u série i aktuálních verzí, serverové odmítnutí bez `confirm_download_category_delete_<id>` nebo `confirm_download_series_delete_<id>` a HTTP důkaz zachování vazeb/logu i potvrzeného cleanupu; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání témat kontaktu a chatu už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má řádkový review dopadu na veřejný kontaktní formulář nebo chat a existující zprávy, serverové odmítnutí bez `confirm_contact_topic_delete_<id>` nebo `confirm_chat_topic_delete_<id>` a HTTP důkaz zachování tématu, vazby zprávy a audit logu i potvrzeného odpojení vazby; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání rezervačních kategorií a míst už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má řádkový review počtu navázaných rezervačních zdrojů, serverové odmítnutí bez `confirm_res_category_delete_<id>` nebo `confirm_res_location_delete_<id>` a HTTP důkaz zachování číselníku, vazby zdroje a audit logu i potvrzeného odpojení vazby; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání kategorií vývěsky a typů akcí už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má řádkový review počtu navázaných položek/odběrů nebo událostí, serverové odmítnutí bez `confirm_board_category_delete_<id>` nebo `confirm_event_type_delete_<id>` a HTTP důkaz zachování taxonomie, vazeb a audit logu i potvrzeného odpojení vazeb; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání blogových kategorií, štítků a sérií článků už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních taxonomických akcí. Má řádkový review počtu navázaných článků, u kategorií i podkategorií, serverové odmítnutí bez `confirm_blog_category_delete_<id>`, `confirm_blog_tag_delete_<id>` nebo `confirm_blog_series_delete_<id>` a HTTP důkaz zachování článků, podkategorií, štítkových/sériových vazeb a audit logu i potvrzeného cleanupu. Ruční NVDA průchod blogové administrace byl potvrzený 2026-07-09 bez nahlášené regrese; opakovat při změnách blogových taxonomií a sérií.

Poznámka 2026-07-09: mazání FAQ kategorií už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních taxonomických akcí. Má řádkový review počtu navázaných otázek a podkategorií, serverové odmítnutí bez `confirm_faq_category_delete_<id>` a HTTP důkaz zachování otázek, podkategorií a audit logu i potvrzeného cleanupu; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání celého blogu už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních blogových akcí. Má řádkový review počtu článků, kategorií, štítků, sérií a týmových přiřazení, serverové odmítnutí bez `confirm_blog_delete_<id>`, transakční potvrzený průchod a HTTP důkaz zachování blogu, obsahu, taxonomií, sérií, týmu a audit logu bez potvrzení. Ruční NVDA průchod blogů byl potvrzený 2026-07-09 bez nahlášené regrese; opakovat při změnách blogového mazání, přesunů obsahu nebo týmových vazeb.

Poznámka 2026-07-09: mazání rezervačního zdroje už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních rezervačních akcí. Má řádkový review počtu budoucích nezrušených rezervací, vazeb na místa, pravidel otevírací doby, slotů a blokovaných dnů, serverové odmítnutí bez `confirm_res_resource_delete_<id>`, transakční potvrzený průchod a HTTP důkaz zachování rezervace, dostupnosti, zdroje a audit logu bez potvrzení; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu.

Poznámka 2026-07-09: mazání celého Form Builder formuláře už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má řádkový review veřejné URL, počtu polí, odpovědí, historie odpovědí a dopadu na přílohy, serverové odmítnutí bez `confirm_form_delete_<id>`, transakční potvrzený cleanup formuláře/polí/odpovědí/historie a HTTP důkaz zachování dat i audit logu bez potvrzení; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu v přehledu formulářů.

Poznámka 2026-07-09: individuální mazání odpovědi Form Builderu už není součástí otevřeného základního `3.3.4` rizika individuálních destruktivních akcí. Má review v přehledu odpovědí i detailu odpovědi s referencí, stavem, počtem interní historie a příloh, serverové odmítnutí bez `confirm_form_submission_delete_<id>`, transakční potvrzený cleanup odpovědi/historie a HTTP důkaz zachování dat i audit logu bez potvrzení; zůstává jen pozdější ruční NVDA/keyboard-only ověření oznámení review textu, checkboxu a chybového stavu v přehledu i detailu odpovědi.

Poznámka 2026-07-10: editory podcastových kapitol, osob a platforem už nejsou součástí otevřené základní mezery `3.3.3`. Používají serverově renderované field-level chyby s konkrétním návrhem opravy, zachovávají odeslané hodnoty a mají zdrojový i HTTP regresní důkaz v `podcast_source_guardrails` a `podcast_admin_validation_http`. Ručně zbývá ověřit oznámení alertu a návrat k dotčenému poli s NVDA/keyboard-only; obecný copy pass pokračuje nad jinými méně častými moduly.

## Nízká priorita

| Oblast | Kritéria | Riziko | Doporučený další krok |
|---|---|---|---|
| Language of parts | 3.1.2 | Redakční checklist popisuje použití `lang` a čistý HTML editor má helper pro vložení `<span lang="…">`; stejný helper je dostupný i u veřejně renderovaných taxonomických HTML popisů. Systém ale označení nevynucuje a reálný author content zatím není ručně ověřený. | Ručně ověřit author content s cizojazyčnými citacemi, použitelnost helperu s klávesnicí/NVDA, taxonomické popisy v publikovaném obsahu a zvážit pozdější obsahový audit chybějících `lang` atributů. |
| Consistent Help | 3.2.6 | Administrace má jednotnou stránku `Nápověda a podpora` ve stabilní spodní navigaci a veřejná šablonová vrstva má footer navigaci `Pomoc a kontakt` pro Kontakt/Chat. Automatizované guardraily hlídají admin, default public baseline i portable/static theme inheritance; otevřené zůstává ruční potvrzení a případné provozní PHP úpravy mimo portable model. | Ručně ověřit admin i veřejnou help navigaci s klávesnicí/NVDA a u ručně psané nebo provozně upravené PHP šablony projít patičku jako samostatnou výjimku. |
| Author content governance | 1.1.1, 1.2.x, 3.1.2 | Checklist pro editory existuje, ale jeho reálné použití v redakčním workflow zatím není ručně ověřené na reprezentativním obsahu. | Projít reprezentativní články, stránky, galerie, podcasty, video snippety a externí embedy podle `author-content-checklist.md`. |

## Evidence pravidla

- Každá položka backlogu musí po opravě aktualizovat `wcag-22-aa-conformance.md`.
- Pokud oprava mění chování CMS, doplní se test nebo audit guardrail.
- Pokud oprava vyžaduje nová data, postupuje se end-to-end přes instalaci, migraci, export/import, schema parity a dokumentaci.
- Pokud se rozhodne, že problém zůstává odpovědností autora obsahu, musí být jasně popsán v dokumentaci.
- Nový modul nebo větší modulové rozšíření musí před implementací ověřit, zda nezhoršuje některou položku tohoto backlogu. Pokud přidává nový rizikový vzor, například média, captcha/auth flow, dialog, upload, tabulku, časový limit nebo autorem dodávaný obsah, doplní se nová položka backlogu nebo odpovídající řádek ve WCAG matici.

## Uzavřená evidence

- 2026-07-09: Širší ruční NVDA průchod bez nahlášené regrese pokryl blogy, nastavení, dlouhé administrační formuláře, odstraňování a přeskupování widgetů, statické stránky i stránky blogu, statistiky, hledání v administraci včetně command palety, focus order a připínání/odepínání položek z osobního dashboardu. Tato evidence uzavírá samostatné screen-reader follow-upy pro uvedené workflow; vizuální 400% zoom, kontrast, hover/ikonové stavy a custom theme varianty zůstávají otevřené jako samostatná ruční práce.
- 2026-07-04: Prioritní browser průchod admin reflow při 320 px ověřil media, widgets, statistics, Form Builder a přehled formulářů. Nalezený globální horizontální scroll ve statistikách a přehledu formulářů byl opraven přes `.table-responsive`, posílené CSS containment, cache-busting `admin/assets/layout.css` a wrapper skrytých datových tabulek grafů; runtime `admin_mobile_reflow_guardrails` teď hlídá i forms overview a cache-busting.
- 2026-07-05: Administrace má jednotný help entrypoint `Nápověda a podpora` dostupný ze stabilní spodní navigace. `admin/help.php` inventarizuje command centrum, profil, redakční checklist, kontaktní/chat/Form Builder flow, provozní nastavení a návaznou dokumentaci; runtime `admin_consistent_help_guardrails` a HTTP `admin_consistent_help_http` hlídají route guard, link order, heading-backed sekce a render.
- 2026-07-05: Výchozí veřejná šablona má ve footeru pojmenovanou navigaci `Pomoc a kontakt`, která se vykreslí při zapnutém Kontakt nebo Chat modulu a drží stabilní pořadí Kontakt, Chat. Runtime `public_consistent_help_guardrails` a HTTP `public_consistent_help_http` hlídají markup, CSS, module gating i render na homepage a vyhledávání.
- 2026-07-10: Portable/static theme varianty mají pro `3.2.6 Consistent Help` automatizovaný inheritance guardrail: unit testy odmítají `layouts/`, `partials/`, `views/` a PHP assety v portable theme package, runtime `public_consistent_help_guardrails` hlídá static package kontrakt i absenci PHP override bucketů v ne-default themes a HTTP `public_consistent_help_http` renderuje ne-default šablonu se stejnou footer navigací `Pomoc a kontakt`.
- 2026-07-04: Audio/video snippety mají guardrail pro mediální textové alternativy: audio i video podporují `transcript` odkaz, přímý video shortcode podporuje WebVTT titulky přes `<track kind="captions">` a knihovna médií přijímá `.vtt` soubory. Unit testy a runtime render článku hlídají výsledné HTML.
- 2026-07-05: Přímý video shortcode podporuje WebVTT audio-description stopu přes atribut `descriptions` a vykresluje `<track kind="descriptions">`. Unit testy a runtime `content_shortcodes` hlídají platný `.vtt` soubor, label i veřejný render vedle titulků a přepisu.
- 2026-07-04: Vznikl `docs/accessibility/author-content-checklist.md`, který editorům dává praktickou kontrolu alt textů, obrázků s textem, přepisů, titulků, jazyka částí, odkazů, nadpisů, tabulek, barev, vlastního HTML a externích embedů. Runtime audit hlídá existenci checklistu a odkazy z hlavní dokumentace.
- 2026-07-04: Veřejný kontakt a Food objednávka předvyplňují kontaktní údaje přihlášeného veřejného uživatele přes `currentUserContactDefaults()`; runtime `redundant_entry_guardrails` a HTTP integrace hlídají helper, napojení i render jména, e-mailu a telefonu.
- 2026-07-06: Přihlášené veřejné rezervace nevyžadují opakované zadání kontaktních údajů ani captchu a při uložení vytvářejí kontaktní snapshot z profilu přes `currentUserContactDefaults()`. Runtime `redundant_entry_guardrails` hlídá napojení controlleru a HTTP `reservations_http` ověřuje render bez kontaktních polí/captchy i uložené jméno, e-mail a telefon z profilu.
- 2026-07-09: Veřejný chat, odpovědi v chatovém vlákně a blogové komentáře předvyplňují u přihlášeného veřejného uživatele jméno a e-mail z profilu přes `currentUserContactDefaults()` a při validační chybě zachovávají ručně odeslané hodnoty. Runtime `redundant_entry_guardrails` hlídá napojení controllerů i `autocomplete="name"` a HTTP `chat_topics_threads_support_http` / `blog_related_articles_http` ověřují render předvyplnění.
- 2026-07-04: Sdílený čistý HTML editor má helper `Jazyk části textu`, který vloží `<span lang="…">` kolem vybraného cizojazyčného textu. Runtime `content_snippet_guardrails` hlídá nápovědu, formulářové ovládání, JS vložení, live status a CSS helperu.
- 2026-07-10: Helper `Jazyk části textu` je dostupný i u veřejně renderovaných taxonomických HTML popisů: blogové kategorie, štítky a série, kategorie vývěsky, typy akcí a kategorie/série Ke stažení. Runtime `content_snippet_guardrails` propojuje tyto veřejné `renderContent()` výstupy s odpovídajícími admin editory a hlídá `adminHtmlSnippetSupportMarkup()` i content picker u create/edit polí.
- 2026-07-04: Podcastové epizody mají nové pole `transcript` pro přepis epizody. Přepis se ukládá v instalaci/migraci, exportu/importu a revizích, veřejný detail ho vykresluje jako samostatnou sekci a unit/runtime guardraily hlídají fallback excerptu i veřejný výstup.
- 2026-07-04: Navazující browser průchod admin reflow při 320 px ověřil comments, contact, chat, reservations, food, downloads, gallery, importy a content picker. Nalezený root overflow ve food/downloads/gallery a main-level scroll u contact/chat byl opraven lokálními `.table-responsive` wrappery; sekundární odkazy v `.button-row` a `.actions` mají minimální target height. Runtime `admin_mobile_reflow_guardrails` teď hlídá i tyto husté moduly.
- 2026-07-04: Browser průchod reprezentativních dlouhých admin formulářů při 320 px ověřil page/blog/news/event/download/gallery/food/board/FAQ/place/polls/reservations/podcast. Nalezený main-level scroll způsobený min-content šířkou fieldsetů byl opraven sdíleným CSS, rezervační otevírací doba a podcastové přehledy dostaly `.table-responsive` wrappery a přímé action odkazy v odstavcích mají minimální target height. Runtime `admin_mobile_reflow_guardrails` teď hlídá i tyto kontrakty.
- 2026-07-04: `1.4.10 Reflow`, `2.4.11 Focus Not Obscured` a `2.5.8 Target Size` mají runtime `admin_mobile_reflow_guardrails`, který hlídá stackování admin navigace na mobilní šířce, lokální scroll datových tabulek, one-column collapse media/Form Builder/statistics gridů, flexibilní action rows a minimální rozměr řadicích ovladačů, sekundárních action odkazů i přímých action odkazů v odstavcích; po browser průchodech prioritních, hustých tabulkových a reprezentativních dlouhých formulářových stránek zůstává práce na 400 % zoomu, custom modulech a asistivních kombinacích.
- 2026-07-04: `1.4.3 Contrast (Minimum)`, `1.4.11 Non-text Contrast` a `2.4.7 Focus Visible` mají nový runtime `contrast_focus_guardrails`, který měří default/admin/login textové páry, stavové hlášky, focus tokeny, skip link a hranice inputů/tlačítek; zbylá práce je ruční měření custom theme, hover/disabled, ikon a progress stavů.
- 2026-07-04: `3.3.8 Accessible Authentication (Minimum)` je po automatizovaných guardrailech a ručním potvrzení auth flow vedené jako `Supports`; při změnách loginu, registrace, 2FA, tokenového resetu nebo session timeoutů se ruční scénář z `manual-test-protocol.md` opakuje.
- 2026-07-04: command centrum, widget dialog a content/media picker prošly ručním NVDA/keyboard-only ověřením bez regrese; při změnách JS dialogů se znovu ověřuje Escape, Tab focus smyčka, návrat fokusu a oznamovaný stav ovládacích prvků.
- 2026-07-04: `1.3.5 Identify Input Purpose` má automatizovaný guardrail pro auth metadata, veřejný kontakt, food objednávky, guest rezervace, veřejný chat, blogové komentáře a Form Builder renderer. Form Builder nově používá sdílený helper pro `email`, `tel`, `url`, zjevné jmenné textové pole a organizaci; HTTP integrace ověřuje render kontaktu, food objednávky a veřejného formuláře.
- 2026-07-04: `1.4.12 Text Spacing` má runtime `text_spacing_guardrails`, který v core CSS hlídá záporné `letter-spacing`, `text-overflow: ellipsis`, line clamp a `!important` zámky na text-spacing vlastnostech. Default, modern-service a editorial šablona už nepoužívají záporné prostrkání nadpisů a administrační SEO preview dlouhé titulky/popisy zalamuje místo ořezu.
- 2026-07-04: `3.3.3 Error Suggestion` má sdílený helper pro chybu veřejné matematické ověřovací otázky. Kontakt, newsletter subscribe, odběr vývěsky, Food objednávkové poptávky a Form Builder zobrazují konkrétní návrh opravy a runtime `public_error_suggestion_guardrails` s HTTP integrací hlídají, aby se nevrátila pouze generická hláška.
- 2026-07-04: Guest rezervační formulář používá stejný `publicCaptchaErrorMessage()` pro chybnou ověřovací otázku a mapuje field-level chyby na jméno, e-mail, telefon, počet osob, slot/čas i captcha. HTTP `reservations_http` ověřuje render chyby s návrhem opravy, `aria-invalid` a absenci uložené rezervace po chybné captche.
- 2026-07-04: Veřejný Form Builder už u custom polí nepoužívá pouze obecné povinné/formátové texty. Povinný text, výběr, souhlas a upload, e-mail, URL, nepovolená možnost a upload chyba radí konkrétní opravu; unit testy helperů, runtime `public_forms_http_guardrails` a HTTP `public_form_submit_http` hlídají návrat.
- 2026-07-04: Admin URL validační chyby u zajímavých míst, podcastových pořadů a podcastových epizod už nevypisují jen „platný formát“. Field-level texty radí použít http/https adresu nebo doménu bez schématu, připomínají automatické uložení jako `https://` a u volitelných polí říkají, kdy je nechat prázdná; runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-04: Admin plánovací datum/čas chyby u statických stránek, článků, novinek, událostí, anket, rezervačních zdrojů a podcastových epizod už nevypisují pouze „neplatný formát“. Texty radí vybrat hodnotu v poli datum a čas, volitelné plánování nechat prázdné, odstranit prázdný řádek nebo opravit pořadí začátku/konce; runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-05: Editor rezervačního zdroje už pro klientské chyby hromadného generátoru slotů a přidání blokovaného dne nepoužívá prohlížečový `alert()`. Chyby se zapisují do inline textových `role="alert"` prvků s konkrétním návrhem opravy a runtime `admin_field_error_guardrails` hlídá markup, helper i zákaz návratu starých alertů.
- 2026-07-05: Sdílené administrační AJAX řazení už při selhání uložení nepoužívá prohlížečový `alert()`. Úspěch i chyba se zapisují do textové inline hlášky s `role="status"` / `role="alert"`, `aria-atomic="true"` a runtime `admin_field_error_guardrails` hlídá návrat starého alertu i chybějící DOM hlášku.
- 2026-07-05: Detaily kontaktních zpráv, chat zpráv a odpovědí Form Builderu už při neplatné e-mailové odpovědi nezůstávají jen u souhrnného textu. Předmět i text odpovědi dostanou field-level chybu přes `aria-describedby`, konkrétní návrh opravy a `aria-invalid`; runtime `admin_field_error_guardrails` hlídá všechny tři šablony a HTTP integrace render kontaktu a chatu.
- 2026-07-05: GitHub issue bridge v detailu odpovědi Form Builderu už při chybě vytvoření nebo napojení issue nezůstává jen u obecného alertu. Repozitář, název issue, tělo issue a adresa existující issue dostávají field-level chybu přes existující `aria-describedby`, konkrétní návrh opravy a `aria-invalid`; runtime `admin_field_error_guardrails` a HTTP `form_issue_preset_http` hlídají render i zákaz návratu starých generických hlášek.
- 2026-07-05: Editor Form Builderu už u hlavních PRG chyb názvu, slugu, notifikačního e-mailu, potvrzovacího e-mailového pole a webhook URL nezůstává u obecného souhrnného textu. Souhrnný alert říká, že oprava je u dotčeného pole, field-level text radí konkrétní krok a HTTP `form_issue_preset_http` renderuje všechny tyto `err` stavy.
- 2026-07-05: Editor statických stránek už u hlavních PRG chyb názvu, slugu, přiřazeného blogu, plánovaného publikování a plánovaného zrušení publikace nezůstává u obecného souhrnného textu. Souhrnný alert ukazuje na dotčené pole, field-level text radí konkrétní opravu a HTTP scénář blogových statických stránek renderuje všechny tyto `err` stavy.
- 2026-07-05: Editor FAQ už u hlavních PRG chyb otázky, odpovědi a slugu veřejné stránky nezůstává u obecného souhrnného textu. Souhrnný alert je atomický, field-level text radí konkrétní opravu a HTTP `faq_categories_feedback_http` renderuje stavy `err=required` a `err=slug`.
- 2026-07-05: Editor FAQ kategorií už u hlavních chyb názvu, slugu a meta title nezůstává u obecného souhrnného textu. Souhrnný alert je atomický, field-level text radí doplnit krátký název, použít slug s písmenem/číslem, zvolit jiný unikátní slug nebo zkrátit meta title a HTTP `faq_categories_feedback_http` renderuje tyto stavy.
- 2026-07-05: Editory blogových kategorií a štítků už u povinného názvu a duplicitního slugu nezůstávají u obecného souhrnného textu. Souhrnný alert je atomický, field-level text radí krátký název, jiný unikátní slug nebo prázdné slug pole pro automatické vytvoření a HTTP `blog_taxonomy_landing_http` renderuje tyto stavy.
- 2026-07-05: Série článků, externí odkazy blogu, základní chybové stavy rezervačních zdrojů, podcastové pořady a newsletter composer už nezůstávají u obecných validačních textů. Souhrnné alerty jsou atomické, dotčená pole dostávají `aria-invalid`, `aria-describedby` na existující nápovědu nebo field-level chybu a HTTP scénáře `blog_article_series_http`, `blog_static_pages_http`, `reservations_http`, `podcast_admin_validation_http` a `newsletter_compose_validation_http` hlídají render i zákaz návratu starých textů.
- 2026-07-05: Hlavní navigační externí odkazy, přesměrování, alba galerie, fotografie galerie a ruční vytvoření rezervace už mají atomické souhrnné alerty a field-level návrhy oprav pro povinný název, interní cesty/URL, duplicitní nebo nepoužitelný slug, nadřazené album, licenci, datum pořízení, zdroj, uživatele/hosta, datum/čas a konflikt rezervace. Runtime `admin_field_error_guardrails` a HTTP scénáře `forms_nav_http`, `redirects_validation_http`, `gallery_metadata_http` a `reservations_http` hlídají render, existující `aria-describedby` cíle, zachování hodnot a zákaz návratu obecných textů.
- 2026-07-05: Editor článku, správa blogů, položky vývěsky, jídelní/nápojové lístky a ankety mají atomické souhrnné alerty a field-level návrhy oprav pro titulek/název/text, slug, cílové blogové vazby, kategorii, datum vyvěšení, možnosti odpovědi a vícevýběrový limit. Runtime `admin_field_error_guardrails` a HTTP scénáře `blog_related_articles_http`, `blog_management_validation_http`, `board_save_http`, `food_structured_items_http` a `poll_voting_modes_http` hlídají render i zákaz návratu obecných textů.
- 2026-07-05: Editory novinek, událostí, míst, položek ke stažení a podcastových epizod mají atomické souhrnné alerty a field-level návrhy oprav pro povinné názvy/texty, slug, datum začátku a pořadí termínů, registrační URL, typ akce, vazbu na místo, opakování, souřadnice, sérii ke stažení a obrázek epizody. Runtime `admin_field_error_guardrails` a HTTP scénáře `news_editor_validation_http`, `events_types_places_recurrence_http`, `downloads_catalog_versions_http` a `podcast_admin_validation_http` hlídají render i zákaz návratu obecných textů.
- 2026-07-05: Profil správce, správa uživatelských účtů, nastavení webu a správa šablon mají atomické souhrnné alerty a field-level návrhy oprav pro e-mail, heslo, kontrolní heslo, autora, TOTP kód, název webu, veřejný název vývěsky, GitHub repozitář, logo/faviconu, aktivní šablonu, theme settings a theme ZIP import/export. Runtime `admin_field_error_guardrails` a HTTP `admin_validation_a11y_http` hlídají zdroj i render.
- 2026-07-06: Vizuální administrační guardraily byly posílené pro `1.4.3`, `1.4.10`, `1.4.11`, `2.4.11` a `2.5.8`: dark-mode info panely a inline štítky mají měřené tokeny, forced-colors režim má fallback pro focus/skip/ovládací prvky/panely/karty, focus i anchor cíle mají scroll-margin, disabled ovládací prvky se nedimují opacity, checkbox/radio ovladače mají minimální velikost, kompaktní řadicí cíle mají 28 px a mobilní baseline hlídá i theme/checkbox/summary gridy a vybrané flex řádky.
- 2026-07-06: `2.2.1 Timing Adjustable` má nový automatizovaný guardrail pro re-auth ochranu dlouhých admin editací: autosave při submitu ukládá recovery kopii, content-lock heartbeat po vypršení session vrací JSON 401 a live-region upozornění a admin login při návratu na původní stránku oznamuje lokální záložní koncept. Runtime `session_security_guardrails` a HTTP scénáře `admin_json_post_only_endpoints_http` / `admin_login_redirect_http` hlídají zdroj i render; backlog nechává otevřený už jen ruční reálný timeout a storage edge cases.
- 2026-07-06: `1.3.5 Identify Input Purpose` má rozšířený Form Builder guardrail pro pracovní pozici, datum narození a jasně pojmenovaná adresní pole včetně ulice, adresních řádků, PSČ, města, kraje a země. Unit testy hlídají helper včetně negativního případu `Adresa webu`, runtime audit hlídá mapování a HTTP integrace ověřuje render tokenů ve veřejném formuláři.
- 2026-07-06: `3.3.4 Error Prevention` má nový automatizovaný guardrail pro trvalé smazání v administrativním Koši: řádek slouží jako review typ/název/datum, purge formulář má `fieldset`/`legend`, viditelnou nevratnost, potvrzovací checkbox, item-specific potvrzení a serverovou kontrolu. HTTP `trash_error_prevention_http` ověřuje render, odmítnutí nepotvrzeného purge i potvrzené trvalé smazání.
- 2026-07-06: Správa uživatelů má navazující error-prevention guardrail pro změnu role existujícího účtu: formulář zobrazuje review aktuální a nové role, změna vyžaduje potvrzovací checkbox a server odmítne nepotvrzený požadavek bez uložení role. Runtime `admin_field_error_guardrails` hlídá zdrojový kontrakt a HTTP `admin_validation_a11y_http` ověřuje odmítnutí i potvrzenou změnu.
- 2026-07-06: Newsletter composer má navazující error-prevention guardrail pro odeslání rozesílky: formulář zobrazuje review počtu potvrzených a čekajících odběratelů, odeslání vyžaduje potvrzovací checkbox a server validní rozesílku bez potvrzení odmítne, aniž ji zapíše do historie. Runtime `admin_field_error_guardrails` hlídá zdrojový kontrakt a HTTP `newsletter_compose_validation_http` ověřuje render i odmítnutí nepotvrzené rozesílky.
- 2026-07-06: Hromadné akce nad odběrateli newsletteru mají navazující error-prevention guardrail: přehled zobrazuje review text pro vybrané odběratele, všechny bulk akce vyžadují potvrzovací checkbox a server odmítne nepotvrzenou akci před změnou dat nebo odesláním e-mailu. Runtime `admin_field_error_guardrails` hlídá zdrojový kontrakt a HTTP `newsletter_bulk_error_prevention_http` ověřuje, že nepotvrzený bulk delete nesmaže odběratele.
- 2026-07-08: Individuální mazání odběratele newsletteru má navazující error-prevention guardrail v přehledu i detailu odběratele: formulář popisuje dopad na aktivní odběr a historii rozesílek, vyžaduje `confirm_newsletter_subscriber_delete_<id>` a server odmítne nepotvrzený POST bez změny odběratele nebo audit logu. Runtime `admin_field_error_guardrails` hlídá zdrojový kontrakt a HTTP `newsletter_subscriber_delete_error_prevention_http` ověřuje render, field-level chybu, odmítnutí i potvrzené smazání.
- 2026-07-09: Ruční průchod newsletteru je potvrzený bez zjištěné regrese. Tím už newsletter nezůstává jako samostatná otevřená ruční položka v `3.3.4`; znovu se má projít při změnách veřejného odběru, tokenového potvrzení/odhlášení, composeru, hromadných akcí nebo mazání odběratele.
- 2026-07-06: Importní workflow mají navazující error-prevention guardrail: JSON import, finální WordPress WXR import, XML import z eStránek a dávkové stahování fotografií z eStránek vyžadují review checkbox pro kontrolu zdroje a dopadu. Runtime `admin_field_error_guardrails` hlídá zdrojový kontrakt a HTTP `json_import_utf8_http` ověřuje, že validní JSON import bez potvrzení nezmění nastavení ani newsletter historii.
- 2026-07-06: Hlavní JSON export CMS má navazující error-prevention guardrail: `admin/export.php` nejdřív vykreslí review citlivosti exportu, stažení vyžaduje `confirm_json_export` a server nepotvrzený požadavek odmítne bez attachmentu i audit logu. Runtime audit hlídá zdrojový kontrakt a HTTP `json_export_error_prevention_http` ověřuje review, field-level chybu u checkboxu i potvrzený bezpečný download.
- 2026-07-06: CSV export odpovědí Form Builderu má navazující error-prevention guardrail: `admin/form_submissions.php` nejdřív vykreslí review citlivosti odpovědí a aktuálního filtru, stažení vyžaduje `confirm_form_submissions_csv_export` a server nepotvrzený požadavek odmítne bez attachmentu i audit logu. Runtime audit hlídá zdrojový kontrakt a HTTP `form_submissions_csv_error_prevention_http` ověřuje review, field-level chybu u checkboxu i potvrzený bezpečný download.
- 2026-07-06: CSV export agregovaných výsledků anket má navazující error-prevention guardrail: `admin/polls_results_export.php` nejdřív vykreslí review tématu ankety, typu hlasování, viditelnosti výsledků, počtu hlasujících a agregovaných odpovědí, stažení vyžaduje `confirm_poll_results_csv_export` a server nepotvrzený požadavek odmítne bez attachmentu i audit logu. Runtime audit hlídá zdrojový kontrakt a HTTP `poll_voting_modes_http` ověřuje review, field-level chybu u checkboxu, potvrzený bezpečný download a absenci raw hashů hlasujících.
- 2026-07-06: ZIP export šablony má navazující error-prevention guardrail: `admin/themes.php` popisuje přenos manifestu, uložené vizuální konfigurace a statických assetů, stažení vyžaduje `confirm_theme_export` a server nepotvrzený požadavek odmítne bez attachmentu i audit logu. Runtime audit hlídá zdrojový kontrakt a HTTP `theme_export_error_prevention_http` ověřuje review, field-level chybu u checkboxu i potvrzený bezpečný download.
- 2026-07-06: Hromadné akce nad odpověďmi Form Builderu mají navazující error-prevention guardrail: přehled odpovědí zobrazuje review vybraných odpovědí a zvolené akce, akce vyžaduje `confirm_form_submissions_bulk_action` a server nepotvrzený požadavek odmítne před změnou stavu, trvalým smazáním, odstraněním příloh/historie nebo audit logem. Runtime audit hlídá zdrojový kontrakt a HTTP `form_submissions_bulk_error_prevention_http` ověřuje odmítnutí nepotvrzeného bulk delete i potvrzený průchod.
- 2026-07-06: Hromadné akce nad alby galerie mají navazující error-prevention guardrail: přehled alb zobrazuje review vybraných alb a zvolené akce, smazání i ZIP export vyžadují `confirm_gallery_albums_bulk_action` a server nepotvrzený požadavek odmítne před odstraněním alb/fotek/revizí nebo před odesláním ZIP attachmentu a audit logem. Runtime audit hlídá zdrojový kontrakt a HTTP `gallery_bulk_error_prevention_http` ověřuje odmítnutí nepotvrzeného ZIP exportu, odmítnutí nepotvrzeného bulk delete i potvrzené průchody.
- 2026-07-06: Sdílené hromadné mazání v `bulkActions()` má generic review guardrail: běžné administrační moduly zobrazují text dopadu a checkbox `confirm_bulk_delete`; `admin/bulk.php` odmítne nepotvrzené `delete` akce mimo speciální galerijní flow před cleanupem, smazáním dat nebo audit logem. Runtime audit hlídá zdrojový kontrakt a HTTP `generic_bulk_delete_error_prevention_http` ověřuje FAQ přehled, odmítnutí nepotvrzeného bulk delete bez smazání/logu a potvrzený průchod.
- 2026-07-06: Správa přesměrování má navazující error-prevention guardrail pro individuální smazání: řádková akce už není GET odkaz, ale POST formulář s review staré/nové cesty, checkboxem `confirm_redirect_delete_<id>` a field-level chybou. Server nepotvrzené smazání odmítne bez odstranění záznamu nebo audit logu; HTTP `redirect_delete_error_prevention_http` ověřuje odmítnutí i potvrzený PRG průchod.
- 2026-07-06: Správa uživatelů má navazující error-prevention guardrail pro individuální smazání účtu: řádková akce používá POST formulář s review dopadu na přístup do CMS a osobní administrační zkratky, checkboxem `confirm_user_delete_<id>` a field-level chybou. Server nepotvrzené smazání odmítne bez změny účtu, zkratek nebo audit logu; HTTP `admin_validation_a11y_http` ověřuje odmítnutí i potvrzený cleanup zkratek a audit log `user_delete`.
- 2026-07-06: Knihovna médií má navazující error-prevention guardrail pro individuální smazání nepoužitého média: karta média používá review dopadu na soubor, metadata a odvozené miniatury, checkbox `confirm_media_delete_<id>` a field-level chybu. Server nepotvrzené smazání odmítne před `mediaDeletePhysicalFiles()`, DB smazáním nebo audit logem; HTTP `media_admin_http` ověřuje odmítnutí i potvrzený cleanup fyzického souboru a audit log `media_delete`.
- 2026-07-08: CSV export statistik obsahu má navazující error-prevention guardrail: `admin/statistics.php` nejdřív vykreslí review interních metrik výkonu obsahu, zvoleného období, filtru modulu a počtu řádků, stažení vyžaduje `confirm_statistics_content_csv_export` a server nepotvrzený požadavek odmítne bez attachmentu i audit logu. Runtime audit hlídá zdrojový kontrakt a HTTP `visitor_content_trends_http` ověřuje review, field-level chybu u checkboxu, potvrzený bezpečný download a absenci raw IP hashů, user-agentů nebo referrer tokenů.
- 2026-07-08: Blog má modulovou ACR přílohu `docs/accessibility/modules/blog.md`. Veřejný komentářový formulář u článku nově používá field-level chyby pro jméno, e-mail, text komentáře a captchu, sdílenou akční captcha hlášku, runtime guardrail a HTTP důkaz v `blog_related_articles_http`.
- 2026-07-08: Editor článku má srozumitelnější zařazení do sérií: checkboxový multi-výběr doplňuje textový stav počtu vybraných sérií a tlačítko `Odebrat ze všech sérií`. Runtime audit a HTTP `blog_article_series_http` hlídají, že správce může klávesnicí i čtečkou jasně poznat a vyčistit zařazení článku do sérií.
- 2026-07-04: Admin e-mailové chyby ve vývěsce, událostech, jídelních lístcích, Form Builderu, tématech kontaktu, místech, podcastových pořadech, nastavení, profilu a správě uživatelů už nevypisují pouze „platný formát“. Texty radí úplnou adresu ve tvaru `jmeno@example.cz`, prázdné volitelné pole nebo jedinečnost přihlašovací adresy; runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-04: Admin běžné datumové chyby ve vývěsce, jídelních lístcích a položkách ke stažení už nevypisují pouze „neplatný formát“. Texty radí vybrat kalendářní datum, volitelné pole nechat prázdné nebo opravit pořadí od/do; datum vydání u downloadů má field-level chybu přes `aria-describedby` a runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-04: Admin chyby zdrojů položek ke stažení už nevypisují pouze obecné validační texty. Chybějící zdroj radí lokální soubor nebo externí odkaz, neplatné URL radí http/https nebo doménu bez schématu, volitelná domovská stránka připomíná prázdné pole a checksum vysvětluje přesných 64 znaků `0-9`/`a-f`; runtime `admin_field_error_guardrails` hlídá field-level napojení i návrat generických textů.
- 2026-07-04: Admin obrazové uploady u článků, vývěsky, událostí, míst a položek ke stažení už nevypisují pouze obecné selhání uploadu. Texty radí JPEG/PNG/GIF/WebP, výslovně odmítají SVG a jiné formáty a připomínají prázdné volitelné pole; download náhled má nově field-level chybu přes `aria-describedby` a runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-04: Admin neobrazové uploady u přílohy vývěsky a audio souboru podcastové epizody už nevypisují pouze obecné selhání uložení. Texty radí povolené dokumentové/audio formáty, prázdné volitelné pole a u podcastu možnost použít externí audio URL; runtime `admin_field_error_guardrails` hlídá návrat generických textů.
- 2026-07-08: Knihovna médií a nové souborové pole Form Builderu používají konfigurovatelný limit uploadu z obecných nastavení webu; chybové texty už neslibují pevnou hodnotu 10 MB a runtime/HTTP guardrails hlídají centrální helper.
- 2026-07-04: Knihovna médií už u hromadného uploadu a náhrady souboru nezůstává jen u flash-only chyby. Upload i replacement file input po redirectu dostanou field-level chybu přes `aria-describedby`; text radí podporovaný formát do nastaveného limitu uploadu, zákaz SVG, u náhrady stejnou MIME rodinu a u veřejného souboru stejnou příponu. Runtime `media_library_guardrails` hlídá helper pro field-error flash i konkrétní napojení polí.
- 2026-07-04: Importní formuláře už u JSON exportu, WordPress WXR, eStránky XML a downloaderu fotografií nezůstávají u obecného logu nebo tiché prázdné stránky po POST bez souboru. Chyby se zobrazují jako alert a field-level text přes `aria-describedby`; text radí správný exportní soubor, platné UTF-8 nebo http/https/doménovou URL. Runtime `admin_field_error_guardrails` a HTTP `admin_import_error_suggestions_http` hlídají návrat patternu.
