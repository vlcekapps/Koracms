# Kora CMS Accessibility Conformance Report Draft

Stav: interní draft podle VPAT 2.5Rev WCAG edition. Tento dokument není právní certifikace a má být aktualizován po ručním testování.

## Product Information

- Product: Kora CMS
- Version evaluated: current development branch
- Report type: WCAG 2.2 A/AA product accessibility conformance draft
- Report date: 2026-07-03
- Evaluation methods: source inspection, automated guardrails, runtime audits, HTTP integration scenarios, theme view audit, author-content checklist and manual protocol prepared in `manual-test-protocol.md`
- Standards: WCAG 2.2 Level A and AA
- Source references: W3C WCAG 2 Overview, W3C WCAG 2.2 Quick Reference, W3C WCAG-EM and ITI VPAT 2.5Rev WCAG edition.

## Conformance Levels Used

- Supports: functionality meets the criterion without known defects in the evaluated CMS-provided experience.
- Partially Supports: some functionality supports the criterion, but manual verification, author-supplied content, or known gaps remain.
- Does Not Support: the product does not currently meet the criterion.
- Not Applicable: the criterion does not apply to CMS-generated functionality.

## Notes

Kora CMS is an authoring and publishing system. This ACR separates CMS responsibility from author responsibility. The CMS can provide accessible UI, fields, validation, metadata, safe defaults and an author-content checklist; it cannot guarantee that every manually authored HTML block, uploaded media file, transcript, video caption, alt text, language-of-parts marker, or external embed is accessible.

## WCAG 2.2 Level A

| Criteria | Conformance | Remarks and Explanations |
|---|---|---|
| 1.1.1 Non-text Content | Supports | CMS provides alt text fields, decorative-image support, theme audit coverage and an author-content checklist for alt text and images of text. Author-supplied media still needs editorial review. |
| 1.2.1 Audio-only and Video-only | Partially Supports | Podcast episodes include a dedicated transcript field that renders as a public text alternative for CMS-hosted audio. Audio/video shortcodes also support a guarded `transcript` link, and the author-content checklist describes transcript expectations. Transcript quality and coverage for author-supplied media still require editorial review. |
| 1.2.2 Captions (Prerecorded) | Partially Supports | Direct video shortcodes support WebVTT caption tracks through the `captions` attribute and media uploads allow `.vtt` files. The author-content checklist covers caption timing/quality; external platform captions still require editorial review. |
| 1.2.3 Audio Description or Media Alternative | Partially Supports | Podcast transcripts and audio/video shortcode transcript links provide media alternatives for CMS-authored media. Direct video shortcodes now also support guarded WebVTT audio-description tracks through the `descriptions` attribute and render `<track kind="descriptions">`. Quality, timing and external platform coverage still require editorial/manual review. |
| 1.3.1 Info and Relationships | Supports | Forms, landmarks, tables and sections are covered by runtime and theme guardrails. |
| 1.3.2 Meaningful Sequence | Supports | Shared layouts preserve meaningful DOM sequence; mobile order still needs manual regression testing. |
| 1.3.3 Sensory Characteristics | Supports | Instructions are text-based and do not rely only on color, shape or position. |
| 1.4.1 Use of Color | Supports | Statuses and labels include text, not color alone. |
| 1.4.2 Audio Control | Not Applicable | Core CMS does not autoplay audio. |
| 2.1.1 Keyboard | Partially Supports | Core patterns support keyboard use; command center, widget dialog and content/media picker have automated guardrails for Escape, Tab focus cycling and focus return, with manual no-regression confirmation on 2026-07-04. Dense admin workflows still need manual coverage. |
| 2.1.2 No Keyboard Trap | Supports | Core dialogs are designed with focus trap and focus return; command center, widget dialog and content/media picker were manually confirmed without regression on 2026-07-04. |
| 2.1.4 Character Key Shortcuts | Supports | Global command shortcut uses `Ctrl+K` and is disabled in form fields. |
| 2.2.1 Timing Adjustable | Partially Supports | Admin content-lock refresh now returns a guarded JSON 401 on expired sessions, long admin forms keep a local autosave recovery copy when submitted, and the standalone admin login announces return-to-page/recovery context. Real timeout, multi-tab, 2FA pending and browser storage edge cases still need manual assessment. |
| 2.2.2 Pause, Stop, Hide | Supports | Core does not generate moving or auto-updating visual content requiring pause controls. |
| 2.3.1 Three Flashes or Below Threshold | Supports | Core does not generate flashing content. |
| 2.4.1 Bypass Blocks | Supports | Skip links and main content anchors are present in public/admin/system layouts. |
| 2.4.2 Page Titled | Supports | Shared SEO/theme layers generate page titles. |
| 2.4.3 Focus Order | Partially Supports | Layouts and core dialogs are structured for logical focus, with automated guardrails for returning focus to the trigger in command center, widget dialog and content/media picker. Those dialogs were manually confirmed without regression on 2026-07-04; broader admin keyboard sweeps remain required. |
| 2.4.4 Link Purpose (In Context) | Supports | Links generally use meaningful text and hidden contextual suffixes where needed. |
| 2.5.1 Pointer Gestures | Supports | Core does not require multipoint or path-based gestures. |
| 2.5.2 Pointer Cancellation | Supports | State-changing actions are explicit buttons/POST actions, not down-event-only actions. |
| 2.5.3 Label in Name | Partially Supports | The product avoids replacing visible text with unrelated ARIA labels; icon-only and dynamic controls need manual checks. |
| 2.5.4 Motion Actuation | Not Applicable | Core CMS does not use device motion controls. |
| 3.1.1 Language of Page | Supports | CMS pages use Czech page language in shared layouts. |
| 3.2.1 On Focus | Supports | Focus does not intentionally trigger unexpected context changes. |
| 3.2.2 On Input | Supports | Inputs generally require explicit submit/action for context changes. |
| 3.2.6 Consistent Help | Partially Supports | The admin UI exposes a stable `Nápověda a podpora` help entrypoint in the bottom navigation. The default public theme exposes a heading-backed `Pomoc a kontakt` footer navigation that consistently links to Contact and Chat when those modules are enabled. Runtime and HTTP integration guardrails cover admin link order, read-only route guard, support inventory, public footer rendering, module gating and link order. Manual keyboard/screen-reader confirmation and custom theme review are still pending. |
| 3.3.1 Error Identification | Supports | Field-level and form-level error messaging is guarded. |
| 3.3.2 Labels or Instructions | Supports | Forms use labels, fieldsets, legends and helper text. |
| 3.3.7 Redundant Entry | Partially Supports | Public profile contact details are reused through `currentUserContactDefaults()`: the public contact form and Food order form prefill known name/e-mail/phone values and keep submitted values after validation errors. Signed-in public reservations do not require repeated contact entry or a CAPTCHA, and store the reservation contact snapshot from the same helper. Runtime `redundant_entry_guardrails` and HTTP integration cover public contact, Food order and public reservations, but admin/custom workflow coverage still needs manual redundant-entry testing. |
| 4.1.2 Name, Role, Value | Supports | Controls, dialogs and landmarks are covered by ARIA/name guardrails, including command center `aria-expanded` state, rendered dialog semantics and manual no-regression confirmation for core dialogs on 2026-07-04. |

## WCAG 2.2 Level AA

| Criteria | Conformance | Remarks and Explanations |
|---|---|---|
| 1.2.4 Captions (Live) | Not Applicable | Core CMS does not provide live audio/video streaming. |
| 1.2.5 Audio Description (Prerecorded) | Partially Supports | Podcast transcripts, video transcript links and direct-video WebVTT `descriptions` tracks can be provided and are covered by unit/runtime guardrails. The actual quality of supplied audio-description tracks and external platform support still require editorial/manual review. |
| 1.3.4 Orientation | Supports | CMS does not restrict screen orientation. |
| 1.3.5 Identify Input Purpose | Partially Supports | Authentication fields are covered for `username`, `current-password`, `new-password` and 2FA `one-time-code` metadata. Public contact, food order, guest reservation and generated Form Builder forms now expose guarded autocomplete metadata for name, email, phone, URL, organization, organization title, birth date and clearly named address fields such as street address, postal code, city, region and country. Browser autofill behavior and payment or other specialized custom fields still need manual verification. |
| 1.4.3 Contrast (Minimum) | Partially Supports | Runtime `contrast_focus_guardrails` measures default theme, admin layout and standalone login text/status color pairs against AA thresholds, including admin info panels and inline badges in light and dark mode. Admin disabled controls no longer rely on opacity dimming. Custom theme settings, hover/icon/progress states and author-supplied colors still need manual measurement. |
| 1.4.4 Resize Text | Supports | Layouts are responsive and do not block browser zoom. |
| 1.4.5 Images of Text | Supports | Core UI does not use images of text as controls or headings. The author-content checklist instructs editors to repeat important text from images in HTML content. |
| 1.4.10 Reflow | Partially Supports | Runtime `admin_mobile_reflow_guardrails` covers the shared admin mobile baseline, including table wrappers, long fieldsets, theme/package grids, checkbox grids, summary grids and selected flex rows that collapse at narrow widths. Browser verification on 2026-07-04 at 320 px covered media, widgets, statistics, Form Builder, forms overview, comments, contact, chat, reservations, food, downloads, gallery, import screens, the content picker and representative long edit forms for page/blog/news/event/download/gallery/food/board/FAQ/place/polls/reservations/podcast. Broader 400 % coverage for custom modules and assistive technology combinations is still required. |
| 1.4.11 Non-text Contrast | Partially Supports | Runtime guardrails measure focus indicators plus default/admin/login input, button and admin info-panel borders against the 3:1 non-text threshold. Admin CSS also has forced-colors fallbacks for focus, skip link, controls, panels, cards and selected theme cards. Icons, progress bars, hover states and custom theme variants still need manual verification. |
| 1.4.12 Text Spacing | Partially Supports | Runtime `text_spacing_guardrails` checks core CSS for negative letter spacing, text ellipsis, line clamp and `!important` locks on text-spacing properties; admin SEO preview now wraps long title/description text instead of clipping it. Browser verification with a text-spacing override is still required. |
| 1.4.13 Content on Hover or Focus | Partially Supports | Core avoids hover-only content; dropdowns/tooltips require manual checks. |
| 2.4.5 Multiple Ways | Supports | Navigation, search, sitemap and module listings provide multiple discovery paths. |
| 2.4.6 Headings and Labels | Supports | Heading-backed landmarks and form labels are guarded. |
| 2.4.7 Focus Visible | Supports | Public, admin and standalone login CSS include visible focus styles, and runtime guardrails measure focus token contrast against primary backgrounds. |
| 2.4.11 Focus Not Obscured (Minimum) | Partially Supports | Admin mobile CSS removes the fixed side-by-side sidebar/content layout at narrow widths, keeps command and content picker dialogs within the viewport, and now applies scroll-margin to focused elements and anchor targets. Mobile admin search and representative long edit forms were browser-verified at 320 px. Sticky/anchor interactions and 400 % keyboard/screen-reader combinations still need manual verification. |
| 2.5.7 Dragging Movements | Supports | Reordering uses accessible button alternatives and is not drag-only. |
| 2.5.8 Target Size (Minimum) | Partially Supports | Shared admin CSS guards mobile navigation targets, generic button minimum height, checkbox/radio minimum size, flexible action rows, secondary links in action rows, direct paragraph action links and enlarged compact sort controls. Browser verification at 320 px found no checked standalone controls below 24 px in media/widgets/statistics/Form Builder/forms overview/contact/chat/food/downloads/gallery/content picker and representative long edit forms; inline text-link exceptions, icon-like controls and custom module actions still need manual measurement. |
| 3.1.2 Language of Parts | Partially Supports | Authors can add HTML language attributes, the author-content checklist documents `lang` examples and the plain HTML editor includes a guarded language-of-parts helper that inserts `<span lang="…">` around selected text. CMS still does not enforce author-supplied language markup, so representative content needs editorial/manual review. |
| 3.2.3 Consistent Navigation | Supports | Shared public/admin layouts keep navigation consistent. |
| 3.2.4 Consistent Identification | Supports | Shared module manifests and labels support consistent naming. |
| 3.3.3 Error Suggestion | Partially Supports | Public math verification errors in contact, newsletter, board subscription, Food orders, guest reservations and Form Builder use a shared actionable suggestion and are covered by runtime/HTTP guardrails. Guest reservations now also attach field-level errors to name, email, phone, party size, slot/time and captcha controls. Generated public Form Builder fields provide actionable field-level guidance for required text/select/consent/file fields, invalid email and URL values, invalid offered-choice values and upload type/size errors. Selected admin URL errors for places and podcasts explain accepted http/https or domain-only input and the empty optional-field fallback. Selected admin email errors now suggest a complete `jmeno@example.cz`-style address, leaving optional fields empty or keeping login email addresses unique. Selected admin date errors for board items, food menus and downloads now suggest choosing a calendar date, leaving optional fields empty or correcting from/to ordering. Download source, external URL, project URL, upload file and SHA-256 checksum errors now provide field-level suggestions for local file versus external URL, http/https or domain-only input, optional empty project URL and exact 64-character checksums. Download category/series, board category, event type, contact/chat topic, reservation category/location, article series, blog external-link, reservation resource, podcast show, newsletter composer, main navigation external-link, redirect, gallery album, gallery photo, manual admin reservation, article editor, blog management, board item, food card and poll errors now provide field-level suggestions for missing names/titles/content, unusable or duplicate slugs, invalid URLs/internal paths, parent albums, license URLs, taken-at dates, capacity/reminder values, RSS limits, cover image requirements, missing newsletter content, target-blog relationships, categories, posted dates, poll options, multi-choice limits, resource/user/guest/date/time reservation data, reservation conflicts, overlong category meta titles and preserving typed values after validation failure. Admin profile, user-account, settings and theme-management errors now add the same atomic alert and field-level pattern for login email, passwords, author profile metadata, TOTP, site name, board label, GitHub repository, logo/favicon, active theme, theme settings and theme ZIP import/export. Selected admin image upload errors for articles, board items, events, places and downloads now suggest JPEG/PNG/GIF/WebP, explain that SVG and other formats are rejected, and preserve optional empty image fields; download preview image errors now use field-level feedback. Board attachment and podcast audio upload errors now suggest allowed document/audio formats, optional empty fields and the external audio URL alternative. Media library bulk upload and replacement errors now attach field-level feedback to the file input and suggest supported formats, the configured upload limit, no SVG, same MIME family and same public-file extension where required. JSON import, WordPress WXR, eStránky XML and eStránky photo download errors now attach field-level feedback to file/URL fields and suggest the correct export file, valid UTF-8 or an http/https/domain URL. Selected scheduling date/time errors for articles, pages, news, events, polls, reservation resources and podcast episodes now suggest choosing a valid date/time value, leaving optional fields empty, removing empty rows or correcting start/end ordering. The reservation resource editor now uses text-backed inline alerts with actionable copy for client-side slot-generator and blocked-date validation instead of browser alert dialogs. Shared admin AJAX sorting now writes save failure feedback into inline status/alert text with a retry suggestion instead of a browser alert. A full validation-copy review for remaining admin, custom and less common errors is still pending. |
| 3.3.4 Error Prevention (Legal, Financial, Data) | Partially Supports | CSRF, confirmations, PRG and audit logs exist. CMS content deletion is first reversible through the admin trash; permanent trash purge renders an item-specific review context, requires a server-validated confirmation checkbox, rejects unconfirmed purge requests and records the confirmed action. User role changes also require review of the current/new role plus a server-validated confirmation checkbox before the permission change is saved. Individual user-account deletion now renders row-level impact review text for account access and personal admin shortcuts, requires `confirm_user_delete_<id>`, rejects unconfirmed POST requests before deleting the account, shortcuts or audit log entry, and cleans up shortcuts plus records `user_delete` only after confirmation. Individual unused-media deletion in the media library now renders item-level review text for the file, metadata and derived thumbnails, requires `confirm_media_delete_<id>`, and rejects unconfirmed POST requests before deleting the database row, physical file or audit log entry. Newsletter sends show recipient review counts and reject a valid send request unless the review checkbox is confirmed, without writing a history record. Newsletter subscriber bulk confirm, resend and delete actions also require a server-validated review checkbox; unconfirmed bulk delete is rejected without deleting the subscriber. JSON import, final WordPress WXR import, eStránky XML import and eStránky photo batch download now require a server-validated confirmation checkbox before data or files are written; HTTP integration verifies that an otherwise valid JSON import without confirmation does not change settings or newsletter history. Admin reservation status changes now require action-specific review and a server-validated confirmation checkbox for approve, reject, cancel, complete and no-show actions; unconfirmed requests are rejected before status, history or notification side effects. Manual SQL database backup renders sensitivity review text, requires a server-validated authorization confirmation checkbox, rejects unconfirmed download requests without sending an attachment or writing the audit log, and HTTP integration verifies the confirmed secure attachment path. The main CMS JSON export now follows the same review-and-confirm model: GET renders sensitivity review text, POST without `confirm_json_export` returns a text-backed alert and field-level checkbox error without a download or audit log, and confirmed POST returns the secure attachment. Form Builder submission CSV export also uses this model: GET renders a sensitivity/filter review, POST without `confirm_form_submissions_csv_export` returns a text-backed alert and field-level checkbox error without a download or audit log, and confirmed POST returns the secure CSV attachment. Poll results CSV export renders an aggregated-results review and requires `confirm_poll_results_csv_export`. Content statistics CSV export now also uses the review-and-confirm model: GET renders a review of internal content-performance metrics, date range, module filter and row count, POST without `confirm_statistics_content_csv_export` returns a text-backed alert and field-level checkbox error without a download or `statistics_content_export_csv` audit log, and confirmed POST returns the secure aggregated CSV attachment without raw IP hashes, user agents or referrer tokens. Form Builder submission bulk actions now require a server-validated review checkbox before status changes or permanent deletion; unconfirmed bulk delete is rejected before deleting the response, files/history or writing the audit log. Gallery album bulk delete and gallery ZIP export now share a server-validated review checkbox; unconfirmed delete is rejected before album/photo/revision removal and unconfirmed ZIP export is rejected before attachment or audit logging. Shared `bulkActions()` delete actions for ordinary admin modules now render a generic delete-impact review and require `confirm_bulk_delete`; `admin/bulk.php` rejects unconfirmed generic delete requests before cleanup, record deletion or audit logging, with HTTP coverage in `generic_bulk_delete_error_prevention_http`. Download category and series deletion now also render row-level impact review text for affected downloads and current-version state, require `confirm_download_category_delete_<id>` or `confirm_download_series_delete_<id>`, reject unconfirmed POST requests before clearing relationships, deleting taxonomy rows or writing audit logs, and are covered by `downloads_catalog_versions_http`. Remaining legal/data-critical workflows still need product-level mapping for review, confirmation or undo requirements, especially other sensitive ZIP/specialized exports outside gallery, theme and content-statistics flows, remaining individual destructive actions outside trash/user/redirect/media/download-taxonomy flows and remaining externally impactful operations. |
| 3.3.8 Accessible Authentication (Minimum) | Supports | Registration and password reset request do not require a math CAPTCHA; they use CSRF, rate limiting and honeypot protection. Runtime and HTTP integration guardrails cover no-CAPTCHA auth rendering, password-manager autocomplete metadata, 2FA one-time-code/numeric metadata and text-backed admin/2FA error alerts. Manual password-manager, TOTP, token reset and error-state behavior was confirmed on 2026-07-04. |
| 4.1.3 Status Messages | Supports | Status and error messages use live region semantics and guarded roles. The reservation resource editor keeps client-side validation feedback in named inline alert regions, and shared admin AJAX sorting writes save success/failure feedback into inline status/alert text instead of browser alert dialogs. |

Current `3.3.3` evidence note, 2026-07-05: admin contact, chat and Form Builder reply detail screens now attach an invalid reply subject/body state to both controls with `aria-invalid`, existing `aria-describedby` targets and actionable field-level text. Runtime guardrails cover all three source templates; HTTP integration renders the contact and chat invalid-reply states. The Form Builder GitHub issue bridge now uses the same field-level pattern for missing repository/title/body and invalid existing-issue URLs; runtime and `form_issue_preset_http` verify the rendered alerts and described-by targets. The Form Builder editor now also renders actionable field-level suggestions for title, slug, notification email, submitter confirmation email field and webhook URL errors. The static page editor applies the same pattern to title, slug, blog assignment, publish-at and unpublish-at errors. The FAQ editor now applies the same guardrail to required question/answer and public slug errors. FAQ category editing also renders actionable suggestions for required name, unusable or duplicate slug and overlong meta title. Blog category and tag editors now render actionable suggestions for required names and duplicate slugs. Download category/series, board category, event type, contact/chat topic and reservation category/location editors now render actionable suggestions for required names, unusable or duplicate slugs, overlong category meta titles and preserving typed values after validation failure. Article series, blog external links, reservation resource basics, podcast show basics, newsletter composition, main navigation external links, redirects, gallery albums, gallery photos, manual admin reservation creation, the article editor, blog management, board items, food cards and polls now follow the same atomic-alert and field-level pattern for missing names/content, invalid URLs/internal paths, unusable slugs, target-blog relationships, categories, parent albums, license URLs, taken-at/posted dates, poll options, multi-choice limits, capacity/reminder values, RSS limits, podcast cover requirements, resource/user/guest/date/time data and reservation conflicts. These flows have runtime source checks and rendered HTTP coverage in `faq_categories_feedback_http`, `blog_taxonomy_landing_http`, `downloads_catalog_versions_http`, `board_save_http`, `events_types_places_recurrence_http`, `contact_topics_and_reply_http`, `chat_topics_threads_support_http`, `reservations_http`, `blog_article_series_http`, `blog_static_pages_http`, `blog_related_articles_http`, `blog_management_validation_http`, `forms_nav_http`, `redirects_validation_http`, `gallery_metadata_http`, `food_structured_items_http`, `poll_voting_modes_http`, `podcast_admin_validation_http` and `newsletter_compose_validation_http`.

Additional `3.3.3` ACR evidence, 2026-07-05: news, event, place, download item and podcast episode admin editors now expose atomic form alerts and actionable field-level suggestions for required content, slugs, event timing, event references, recurrence, coordinates, download series and podcast episode images. Runtime `admin_field_error_guardrails` and HTTP scenarios `news_editor_validation_http`, `events_types_places_recurrence_http`, `downloads_catalog_versions_http` and `podcast_admin_validation_http` cover the rendered evidence.

Additional `3.3.3` ACR evidence, 2026-07-05: admin profile, user-account creation/editing, site settings and theme management now expose atomic form alerts and actionable field-level suggestions for account e-mail/password/TOTP errors, author profile metadata, settings basics and branding uploads, active theme selection, theme settings validation and portable theme ZIP import/export. Runtime `admin_field_error_guardrails` and HTTP `admin_validation_a11y_http` cover both static source contracts and rendered invalid states.

Additional visual accessibility evidence, 2026-07-06: admin CSS and runtime guardrails now cover dark-mode info-panel and inline-badge contrast, forced-colors fallback, focused/target scroll-margin, disabled controls without opacity dimming, checkbox/radio minimum size, 28 px compact sort controls and narrow-width collapse for theme/checkbox/summary grids and selected flex rows. This strengthens automated evidence for `1.4.3`, `1.4.10`, `1.4.11`, `2.4.11` and `2.5.8`; manual 400 % zoom, custom module and assistive-technology combinations remain required.

Additional `2.2.1` ACR evidence, 2026-07-06: admin autosave keeps a recovery copy during submit, content-lock heartbeat reports expired sessions through JSON 401 and the existing live region, and the admin login page renders a return notice tied to the login form when a protected admin URL triggered re-auth. Runtime `session_security_guardrails` and HTTP scenarios `admin_json_post_only_endpoints_http` and `admin_login_redirect_http` cover the automated evidence. Manual NVDA/keyboard-only confirmation with a real timeout, 2FA, multiple tabs and disabled/private browser storage remains required.

Additional `1.3.5` ACR evidence, 2026-07-06: Form Builder input-purpose mapping now covers organization title, birth date and clearly named address fields (`street-address`, `address-line1`, `address-line2`, `postal-code`, `address-level2`, `address-level1`, `country-name`) while preserving negative handling for web-address text fields. Unit tests cover helper mapping and HTTP `public_form_submit_http` verifies rendered autocomplete attributes.

Additional `3.3.4` ACR evidence, 2026-07-06: admin Trash permanent deletion now uses a review-and-confirm pattern with item type/title/deleted date, a required confirmation checkbox, item-specific `data-confirm`, server-side rejection of unconfirmed purge requests and PRG feedback. Runtime `admin_field_error_guardrails` covers source/CSS fragments and HTTP `trash_error_prevention_http` covers rendered review text, rejected unconfirmed purge and confirmed purge.

Additional `3.3.4` ACR evidence, 2026-07-06: admin reservation detail status actions now render action-specific review text and confirmation checkboxes for approve, reject, cancel, complete and no-show. `admin/res_booking_save.php` rejects a direct POST without the matching `confirm_reservation_status_*` field before changing status, writing booking history or sending a notification. Runtime `admin_field_error_guardrails` covers the source contract and HTTP `reservations_http` verifies the rendered confirmation, rejected unconfirmed approval and confirmed approval path.

Additional `3.3.4` ACR evidence, 2026-07-06: the main CMS JSON export is no longer a direct GET attachment. `admin/export.php` renders a sensitivity review and requires `confirm_json_export` before download; unconfirmed POST keeps the user on the HTML page with `role="alert"` and a field-level checkbox error, without writing `export_json` to the audit log. Runtime source checks cover the route/capability/form contract and HTTP `json_export_error_prevention_http` verifies review rendering, rejected unconfirmed export and confirmed secure attachment headers.

Additional `3.3.4` ACR evidence, 2026-07-06: Form Builder submission CSV export is no longer a direct `?export=csv` attachment. `admin/form_submissions.php` renders a sensitivity and current-filter review, requires `confirm_form_submissions_csv_export` before download, rejects an unconfirmed POST with `role="alert"` and a field-level checkbox error, and avoids writing `form_submissions_export_csv` until the confirmed download path. Runtime source checks and HTTP `form_submissions_csv_error_prevention_http` verify review rendering, rejected unconfirmed export and confirmed secure attachment headers.

Additional `3.3.4` ACR evidence, 2026-07-06: poll results CSV export is no longer a direct GET attachment. `admin/polls_results_export.php` renders a review of the poll topic, voting mode, result visibility, voter count and aggregated selections, requires `confirm_poll_results_csv_export` before download, rejects an unconfirmed POST with `role="alert"` and a field-level checkbox error, and avoids writing `poll_results_export_csv` until the confirmed download path. Runtime source checks and HTTP `poll_voting_modes_http` verify review rendering, rejected unconfirmed export, confirmed secure attachment headers and the absence of raw voter hash fields.

Additional `3.3.4` ACR evidence, 2026-07-08: content statistics CSV export is no longer a direct GET attachment. `admin/statistics.php` renders a review of internal content-performance metrics, selected date range, module filter, row count and the privacy boundary for aggregated data; download requires `confirm_statistics_content_csv_export`. An unconfirmed POST returns a text-backed atomic alert and field-level checkbox error without sending an attachment or writing `statistics_content_export_csv`; a confirmed POST sends the secure CSV attachment and records the audit log. Runtime source checks and HTTP `visitor_content_trends_http` verify review rendering, rejected unconfirmed export, confirmed secure attachment headers and the absence of raw IP hashes, user-agent fields and referrer tokens.

Additional `3.3.4` ACR evidence, 2026-07-06: theme ZIP export now requires a review of the selected theme and `confirm_theme_export` before download. `admin/themes.php` describes that the portable package transfers the theme manifest, stored visual configuration and static assets, rejects an unconfirmed POST with an atomic alert and field-level checkbox error, and avoids sending the attachment or writing `theme_export` until the confirmed path. Runtime source checks and HTTP `theme_export_error_prevention_http` verify review rendering, rejected unconfirmed export and confirmed secure attachment headers.

Additional `3.3.4` ACR evidence, 2026-07-06: Form Builder submission bulk actions render a review instruction and require `confirm_form_submissions_bulk_action` before changing status or permanently deleting selected responses. `admin/form_submission_bulk.php` rejects unconfirmed POST requests before data changes, attachment/history deletion and audit logging. Runtime source checks and HTTP `form_submissions_bulk_error_prevention_http` verify review rendering, rejected unconfirmed bulk delete and the confirmed path.

Additional `3.3.4` ACR evidence, 2026-07-06: gallery album bulk actions render a review instruction and require `confirm_gallery_albums_bulk_action` before deleting selected albums or exporting them to ZIP. `admin/bulk.php` rejects unconfirmed gallery album delete before album/photo/revision cleanup, and `admin/gallery_export_zip.php` rejects unconfirmed ZIP export before attachment or audit logging. Runtime source checks and HTTP `gallery_bulk_error_prevention_http` verify review rendering, rejected unconfirmed ZIP export, rejected unconfirmed bulk delete and confirmed paths.

Additional `3.3.4` ACR evidence, 2026-07-06: shared `bulkActions()` delete actions render a generic review instruction and require `confirm_bulk_delete` for ordinary admin modules. `admin/bulk.php` rejects unconfirmed generic delete requests outside the special gallery-album flow before cleanup, data deletion or audit logging. Runtime source checks and HTTP `generic_bulk_delete_error_prevention_http` verify FAQ overview rendering, rejected unconfirmed bulk delete without deleting/logging and the confirmed path.

Additional `3.3.4` ACR evidence, 2026-07-06: individual redirect deletion is no longer exposed as a state-changing GET link. `admin/redirects.php` renders a POST review form with the old/new path context, requires `confirm_redirect_delete_<id>`, rejects unconfirmed deletion with a text-backed alert and field-level checkbox error, and avoids both record deletion and `redirect_delete` audit logging until confirmation. Runtime source checks forbid `redirects.php?delete=` and HTTP `redirect_delete_error_prevention_http` verifies review rendering, rejected unconfirmed deletion and confirmed PRG feedback.

Additional `3.3.4` ACR evidence, 2026-07-06: individual user-account deletion in the admin users overview now uses a row-level review-and-confirm pattern. `admin/users.php` describes the access and personal-shortcut impact, requires `confirm_user_delete_<id>`, and renders a text-backed alert plus field-level checkbox error when confirmation is missing. `admin/user_delete.php` explicitly requires `users_manage`, protects the current user and superadmin account, rejects unconfirmed deletion before changing `cms_users`, `cms_admin_shortcuts` or the audit log, and only after confirmation deletes the account, cleans up shortcuts and records `user_delete`. Runtime `admin_field_error_guardrails` covers the source contract and HTTP `admin_validation_a11y_http` verifies rejected unconfirmed deletion and the confirmed cleanup path.

Additional `3.3.4` ACR evidence, 2026-07-06: individual unused-media deletion in the media library now uses the same review-and-confirm pattern. `admin/media.php` describes that the file, metadata and derived thumbnails will be removed, requires `confirm_media_delete_<id>`, and renders an atomic alert plus field-level checkbox error when confirmation is missing. The delete handler rejects unconfirmed POST requests before `mediaDeletePhysicalFiles()`, the `cms_media` delete or `media_delete` audit logging. Runtime `media_library_guardrails` covers the source contract and HTTP `media_admin_http` verifies review rendering, rejected unconfirmed deletion without database/file/log changes and the confirmed delete path.

Additional `3.3.4` ACR evidence, 2026-07-08: Download category and series deletion now use row-level review-and-confirm forms. Category deletion describes the number of affected downloads and requires `confirm_download_category_delete_<id>`; series deletion describes affected downloads and current-version flags and requires `confirm_download_series_delete_<id>`. Unconfirmed POST requests return text-backed alerts and field-level checkbox errors without clearing relationships, deleting taxonomy rows or writing audit logs. Runtime `downloads_source_guardrails` and HTTP `downloads_catalog_versions_http` cover the rendered error state, rejected unconfirmed paths and confirmed cleanup.

Additional `3.3.4` ACR evidence, 2026-07-08: individual newsletter subscriber deletion now uses row/detail review-and-confirm forms. The UI describes removal from active newsletter subscriptions while preserving already-sent newsletter history, requires `confirm_newsletter_subscriber_delete_<id>` and returns a text-backed atomic alert plus a field-level checkbox error when confirmation is missing. The server rejects unconfirmed POST requests before deleting the subscriber or writing the audit log. Runtime `admin_field_error_guardrails` and HTTP `newsletter_subscriber_delete_error_prevention_http` cover overview/detail rendering, rejected unconfirmed deletion without database/log changes and confirmed deletion.

Additional `3.3.4` ACR evidence, 2026-07-09: Contact and Chat topic deletion now use row-level review-and-confirm forms. The UI describes the impact on the public contact form or chat and on existing messages, requires `confirm_contact_topic_delete_<id>` or `confirm_chat_topic_delete_<id>`, and returns a text-backed atomic alert plus a field-level checkbox error when confirmation is missing. The server rejects unconfirmed POST requests before unlinking message relationships, deleting the topic or writing the audit log. Runtime `admin_field_error_guardrails` and HTTP `contact_topics_and_reply_http` / `chat_topics_threads_support_http` cover rendered review text, rejected unconfirmed deletion without message/log changes and confirmed cleanup.

Additional `3.3.4` ACR evidence, 2026-07-09: Reservation category and location deletion now use row-level review-and-confirm forms. The UI describes the number of affected reservation resources and the impact on existing resources/bookings, requires `confirm_res_category_delete_<id>` or `confirm_res_location_delete_<id>`, and returns a text-backed atomic alert plus a field-level checkbox error when confirmation is missing. The server rejects unconfirmed POST requests before unlinking resources, deleting taxonomy rows or writing audit logs. Runtime `admin_field_error_guardrails` and HTTP `reservations_http` cover rendered review text, rejected unconfirmed deletion without relationship/log changes and confirmed cleanup.

Additional `3.3.4` ACR evidence, 2026-07-09: Board category and event type deletion now use row-level review-and-confirm forms. Board category deletion describes affected board items and subscriptions and requires `confirm_board_category_delete_<id>`; event type deletion describes affected events and requires `confirm_event_type_delete_<id>`. Unconfirmed POST requests return text-backed atomic alerts and field-level checkbox errors without unlinking relationships, deleting taxonomy rows or writing audit logs. Runtime `admin_field_error_guardrails` and HTTP `board_save_http` / `events_types_places_recurrence_http` cover rendered review text, rejected unconfirmed paths and confirmed cleanup.

Additional `3.3.4` ACR evidence, 2026-07-09: Blog category, tag and article-series deletion now use row-level review-and-confirm forms. Category deletion describes affected articles and child categories and requires `confirm_blog_category_delete_<id>`; tag deletion describes affected articles and requires `confirm_blog_tag_delete_<id>`; series deletion describes affected articles and requires `confirm_blog_series_delete_<id>`. Unconfirmed POST requests return text-backed atomic alerts and field-level checkbox errors without unlinking articles, child categories, tag/series relationships, deleting taxonomy rows or writing audit logs. Runtime `admin_field_error_guardrails` and HTTP `blog_taxonomy_landing_http` / `blog_article_series_http` cover rendered review text, rejected unconfirmed paths and confirmed cleanup.

Additional `3.3.4` ACR evidence, 2026-07-09: FAQ category deletion now uses a row-level review-and-confirm form. The UI describes affected FAQ questions and child categories, requires `confirm_faq_category_delete_<id>`, and returns a text-backed atomic alert plus a field-level checkbox error when confirmation is missing. The server rejects unconfirmed POST requests before unlinking FAQ questions, moving child categories to the root, deleting the category or writing audit logs. Runtime `admin_field_error_guardrails` and HTTP `faq_categories_feedback_http` cover rendered review text, rejected unconfirmed deletion without relationship/log changes and confirmed cleanup.

## Current Summary

- Supports: strong structural support for landmarks, forms, status messages, navigation, page titles and semantic UI.
- Partially Supports: media alternatives, contrast measurement, keyboard/focus manual sweeps, target size, timeouts and redundant entry.
- Does Not Support: no criteria are currently classified this way in the draft baseline.
- Not Applicable: live media, autoplay audio and motion actuation are not generated by core CMS.

The next revision should update statuses only after executing `manual-test-protocol.md`.
